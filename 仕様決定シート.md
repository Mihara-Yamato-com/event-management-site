# 仕様決定シート（確定版）

**確定日**: 2026/07/27（D-37〜D-39・D-40〜D-42 は 07/28 追加、D-43・D-44 は 07/28 Issue #1 対応で追加）
**状態**: 全45件のうち41件確定（D-20 / D-27 / D-34 / D-39 は廃止により削除）。外部依存2件のみ保留
**用途**: 実装時に `仕様書.md` と本シートの2つを参照する。入力仕様・業務ルール・エラーメッセージの**唯一の正**

---

## 既定案から変更した項目（2件）

| ID | 論点 | 決定 | 変更理由 |
|---|---|---|---|
| **D-12** | カテゴリの選択肢 | **主催者が設定画面で自由に定義する** | Peatix由来の固定6分類（ビジネス／テクノロジー等）は公開マーケットプレイス向けの分類であり、自社イベントの実態に合わない。方針P10に沿う |
| **D-15** | カバー画像の扱い | **アップロード上限2MB。保存前に長辺1200pxへ自動リサイズ** | Blobをbase64化するとJSONエクスポートが約1.33倍に膨らむ。2MB×数十イベントで50MBを超え `JSON.stringify` が破綻する |

**上記2件による追加工数は約0.5h、試験項目は約6件増。** T2-2／T2-3の枠内で吸収する。超過した場合は縮退計画の順0を発動する。

---

## A. 業務ルール（10件・全て既定案で確定）

| ID | 論点 | 決定 |
|---|---|---|
| D-01 | 下書き保存時の必須チェック | **イベント名のみ必須。** 公開時に全必須項目を検証し、不足があれば公開を拒否して不足項目を一覧表示する |
| D-02 | 申込者がいる券種の定員縮小 | **現在の `confirmed` 件数未満には変更不可。** エラーを表示して保存を拒否 |
| D-03 | 申込者がいる券種の削除 | **削除不可。** 申込ゼロの券種のみ削除できる |
| D-04 | 申込者がいるイベントの削除 | 確認ダイアログで申込件数を表示し、承諾時は `entries` も**連動削除** |
| D-05 | 券種ゼロでの公開 | **公開不可。** 最低1件の券種を必須とする |
| D-06 | 申込締切の境界 | `deadline >= 現在時刻` なら申込可（**締切時刻ちょうどは受付ける**） |
| D-07 | 終了判定の境界 | `endAt < 現在時刻` で終了。公開一覧から除外、公開ページは閲覧のみ可・申込不可 |
| D-08 | 下書きイベントへの直接アクセス | **S-14 エラー画面へ。** 下書きは主催者のプレビュー（S-05）からのみ閲覧可 |
| D-09 | キャンセル後の再申込 | **許可する。** 同一メールでの再申込は新規レコードとして扱う |
| D-10 | チェックイン済み参加者のキャンセル | **主催者側のみ可能。** 参加者側からは不可 |

---

## B. 入力仕様 — イベント作成／編集（S-03）

### B-1. 基本情報

| ID | 項目 | 必須 | 形式 | 制約・初期値 |
|---|---|---|---|---|
| D-11 | イベント名 | ● | text | 1〜100文字 |
| D-12 | カテゴリ | ● | select | **`settings.categories` から生成。** 初期値＝リストの先頭 |
| D-13 | 開始日時 | ● | datetime-local | **過去日時も許可**（実績記録の用途） |
| D-14 | 終了日時 | ● | datetime-local | 開始日時より後 |
| D-15 | カバー画像 | | file | `image/png, image/jpeg, image/webp`／**アップロード上限2MB**／**推奨1200×400（横長バナー）**／**保存前に長辺1200pxへリサイズ（JPEG品質0.85）** |

> **カバー画像の表示方針**: 公開ページのヘッダー（横長）とイベント一覧のカード（16:9程度）で異なる比率で表示される。**保存は元の比率を維持し、表示側で `object-fit: cover` による中央トリミングを行う。** 正方形の画像がアップロードされても両方の枠に収まる。

### B-2. 会場・配信

| ID | 項目 | 必須 | 形式 | 制約・初期値 |
|---|---|---|---|---|
| D-16 | 開催形式 | ● | radio | 会場／オンライン。**初期値＝会場** |
| D-17 | 会場名・住所 | ▲ | text | 開催形式＝会場のとき必須。200文字 |
| D-18 | 視聴URL | ▲ | url | 開催形式＝オンラインのとき必須。`http(s)://` で始まること |

### B-3. 詳細

| ID | 項目 | 必須 | 形式 | 制約・初期値 |
|---|---|---|---|---|
| D-19 | 説明文 | ● | textarea | **2000文字。改行のみ有効**（装飾記法なし） |

> **表示時はHTMLエスケープを必須とする。** 改行を `<br>` に変換する場合も、エスケープ後に変換すること。

### B-4. 券種（最低1件必須）

| ID | 項目 | 必須 | 形式 | 制約・初期値 |
|---|---|---|---|---|
| D-21 | 券種名 | ● | text | 50文字。**初期値＝「一般」** |
| D-22 | 定員 | ● | number | **1〜9999の整数。** 0・マイナス・空欄は不可。無制限は認めない（9999で代替） |
| D-23 | 券種の説明 | | text | 200文字 |
| D-24 | 販売開始／終了 | | datetime-local | 初期値＝空欄（制限なし）。指定時は**販売終了 ≦ 申込締切** |
| D-25 | 残席表示の閾値（F-31） | | number | **初期値10。** 残席がこの値以下のとき「残り○枚」を表示。**0 で常に非表示** |

### B-5. 設定・フォーム

| ID | 項目 | 必須 | 形式 | 制約・初期値 |
|---|---|---|---|---|
| D-26 | 申込締切 | ● | datetime-local | **開始日時以前。** 初期値＝開始日時と同じ |
| D-28 | カスタム項目 | | 動的 | **0〜10件。** 項目名50文字、選択肢は1件30文字×最大10件 |
| D-29 | 公開状態 | ● | radio | 下書き／公開。**初期値＝下書き** |

---

## C. 入力仕様 — 申込フォーム（S-06）

| ID | 項目 | 必須 | 形式 | 制約 |
|---|---|---|---|---|
| D-30 | 氏名 | ● | text | 1〜50文字 |
| D-31 | メールアドレス | ● | email | 254文字。`@` を含み前後に文字があること（**簡易チェックに留める**） |
| D-43 | マイチケット用パスワード | ● | password | **4〜16文字の英数字。** S-11 のログインに使う。同じメールアドレスの2回目以降は登録済みのものと照合し、不一致なら申込を受け付けない |

カスタム項目の必須・形式は `formFields` の定義に従う。

---

## D. その他の値

| ID | 論点 | 決定 |
|---|---|---|
| D-32 | 受付コードの形式 | **英大文字＋数字の8桁。** 紛らわしい文字（`O` `0` `I` `1`）を除外。例: `H7K2M9XQ` |
| D-33 | パスコードの形式（F-27） | 4〜16文字の英数字。初回起動時に設定を強制 |
| D-35 | 添付ファイル（F-34・余力） | `pdf, png, jpeg, xlsx, docx`／1ファイル5MB以下／1イベント10件まで |
| D-36 | 一覧の既定の並び順 | イベント一覧＝開始日時の降順／参加者一覧＝申込日時の昇順 |

> **欠番について**: D-20（キャンセル規定）・D-27（キャンセル期限）は **F-32 廃止**、D-34（待機番号の採番）は **F-14 廃止**により削除した。
> 追跡性を保つため再採番はしない。

---

## D'. 組織ブランディング（F-09・余力枠／07-28 追加）

置き場所は **S-09「一般設定」タブ**。実装の優先順位は **ロゴ ＞ ヘッダー画像**。

> **D-39（組織の紹介文）は廃止した。** 参加者トップにキャッチコピーとヒーローを置いたことで、
> 組織の紹介文を出す場所がなくなったため（Issue #4）。

| ID | 項目 | 必須 | 形式 | 制約・初期値 |
|---|---|---|---|---|
| D-37 | 組織ロゴ | | file | `image/png, image/jpeg, image/webp`／2MB以下／**正方形推奨（256×256）／保存前に長辺256pxへリサイズ** |
| D-38 | ヘッダー画像 | | file | 同形式／2MB以下／**推奨1200×400（横長バナー）／保存前に長辺1200pxへリサイズ** |

> **テーマカラー設定は v1.14 で廃止済み。** アクセントカラーは黒固定とし、`--color-accent` の実行時上書きは行わない。

---

## D''. 運用資料と既定フォーム項目（F-34・07-28 追加）

置き場所は **S-16 資料**（組織全体の資料置き場。イベント単位ではない）。

| ID | 項目 | 必須 | 形式 | 制約・初期値 |
|---|---|---|---|---|
| D-40 | 資料ファイル | ● | file | `pdf, png, jpeg, xlsx, docx`／**1ファイル5MB以下**／**登録上限20件**／**リサイズせず原本を保持** |
| D-41 | 資料の説明 | | text | 200文字 |
| D-45 | 資料の紐付け先イベント | ● | select | **どのイベントの資料かを選ぶ。**「組織共通（特定のイベントに紐付けない）」も選択できる。イベント詳細からも同じ資料を開ける |
| D-44 | 運営人員 | | text | 責任者1名＋運営メンバー複数。各「氏名」「担当」とも50文字。**参加者には公開しない**（F-35） |

### D-42 既定の申込フォーム項目（差し替え）

「所属部署」「年数」を削除し、以下を既定とする。

| 項目名 | 型 | 選択肢 |
|---|---|---|
| 参加のきっかけ | select | 社内の案内／知人の紹介／SNS／その他 |
| 懇親会に参加する | checkbox | — |
| 質問・要望 | text | — |

> 自社主催であっても社員限定のイベントとは限らないため、所属部署・年数を既定項目としない。

---

## E. エラーメッセージ

**方針**: 「〜してください」の依頼形。**項目名を必ず含める。** 原因ではなく次の行動を書く。
**実装ごとに言い換えず、下記の文言をそのまま使う。**

| 種別 | 文面 |
|---|---|
| 未入力 | 「{項目名}を入力してください」 |
| 文字数超過 | 「{項目名}は{n}文字以内で入力してください」 |
| 形式不正 | 「{項目名}の形式が正しくありません」 |
| 数値範囲 | 「{項目名}は{min}以上{max}以下で入力してください」 |
| 日時の前後 | 「{項目名}は{基準項目名}より後の日時を指定してください」 |
| 定員縮小（D-02） | 「すでに{n}件の申込があるため、定員を{n}未満にはできません」 |
| 券種削除（D-03） | 「申込のある券種は削除できません」 |
| 券種ゼロで公開（D-05） | 「公開するには券種を1件以上登録してください」 |
| カテゴリ削除（D-12） | 「このカテゴリを使用しているイベントがあるため削除できません」 |
| 画像サイズ超過（D-15） | 「画像は2MB以内のファイルを選択してください」 |
| 満席 | 「この券種は満席です」 |
| 締切後 | 「申込受付は終了しました」 |
| 重複申込 | 「このメールアドレスはすでに申し込まれています」 |
| 日時の前後（以前） | 「{項目名}は{基準項目名}以前の日時を指定してください」 |
| 資料のサイズ超過（D-40） | 「ファイルは5MB以内のものを選択してください」 |
| 資料の形式不正（D-40） | 「対応していないファイル形式です」 |
| 資料の件数超過（D-40） | 「資料は20件以内で登録してください」 |
| パスワードの形式（D-43） | 「マイチケット用パスワードは4〜16文字の英数字で入力してください」 |
| パスワードの不一致（D-43・申込時） | 「このメールアドレスには別のパスワードが登録されています。前回設定したものを入力してください」 |
| ログイン失敗（D-43・S-11） | 「メールアドレスまたはパスワードが正しくありません」（**登録の有無を区別しない**） |

---

## F. デモデータの内容（F-21）

**目的**: 結合試験の前提条件を1操作で作れるようにする。試験工数に直結する。

| 対象 | 内容 |
|---|---|
| イベント | **3件。** 公開2件（うち1件は満席・1件は空きあり）／下書き1件。1件はシリーズ設定あり |
| 券種 | 満席イベントに2券種（一方のみ満席）、他は1券種 |
| 参加者 | **計20件。** `confirmed` 18／`cancelled` 2。うち5件はチェックイン済み（**`waitlist` は F-14 廃止により存在しない**） |
| カスタム項目 | 1イベントに3項目（D-42 の既定項目＝参加のきっかけ／懇親会に参加する／質問・要望） |
| カバー画像 | **なし**（空状態の表示確認も兼ねる） |
| カテゴリ | 初期カテゴリ6件を投入 |

---

## G. 外部依存で保留中（自分では決められない）

| ID | 事項 | 期限 | 状態 |
|---|---|---|---|
| Q7 | 結合試験仕様書のフォーマット（GantForge形式のサンプル入手） | 7/29 | 未取得 |
| H-1〜H-4 | 自社運用の要件（検討記録 4章） | 7/28 午前 | ヒアリング中 |

**H-1〜H-4 が 7/28 午前までに確定しない場合は余力枠として扱い、実装計画に組み込まない。**

---

## D-12 に伴う仕様書への追記事項

### データモデル（仕様書 6章）— `settings` に追加

| key | 値 | 機能 |
|---|---|---|
| `categories` | `string[]`（**上限20件**） | F-01 / F-26 |

**初期値**（初回起動時に投入。空リストではイベントを作成できないため必須）

```
["セミナー", "勉強会", "研修", "説明会", "懇親会", "その他"]
```

### 判定ルール（仕様書 6章）— 追加

- **使用中カテゴリは削除不可。** 当該カテゴリを持つイベントが1件以上ある場合、削除を拒否する（D-03と同じ方針で統一）
- カテゴリの登録上限は20件

### 画面一覧（`画面一覧.md`）— S-09 の役割に追記

S-09「一般設定」タブに **カテゴリ管理（追加・削除・並べ替え）** を含める。

### 機能一覧（`機能一覧.md`）— F-26 の記述を修正

カテゴリ絞り込みの選択肢を **`settings.categories` 由来** に変更する。

---

## 試験書へ流用する項目（T4-4 で使用）

### 相関チェック（異常系4件）

1. 終了日時 ≦ 開始日時 → エラー
2. 申込締切 > 開始日時 → エラー
3. 販売終了 > 申込締切 → エラー
4. 定員 < 現在の申込数（編集時） → エラー

### 業務ルール（異常系・境界値10件）

A節 D-01〜D-10 がそのまま試験項目になる。特に境界値として確認すべきもの:

- 締切時刻**ちょうど**の申込 → 受付ける（D-06）
- 終了時刻**ちょうど**のイベント → まだ終了扱いにしない（D-07）
- 定員 `0` および `10000` の入力 → エラー（D-22）
- 定員を現在の申込数と**同数**に変更 → 許可（D-02）

### D-12・D-15・D-37〜D-39 起因（追加8件）

- 使用中カテゴリの削除 → エラー
- カテゴリを21件目登録 → エラー
- カテゴリを全削除した状態でイベント作成 → 作成不可
- 2MB超の画像アップロード → エラー
- 2MB以内の大きい画像 → 長辺1200pxに縮小されて保存される
- **正方形の画像** → 公開ページ・カードの両方で崩れず表示される
- 画像を含むイベントのJSONエクスポート／インポート → 画像が復元される
- 組織ロゴ未設定時 → 組織名テキストのみ表示される（F-09 未実装時の既定挙動）

**合計22項目。** 試験書80項目のうち約3割の骨格がここで確定している。

---

## 実装時の参照ルール

1. 入力仕様・業務ルール・エラーメッセージで迷ったら**まず本シートを見る**。再発明しない
2. 本シートに記載がない場合は「**自社のイベント運営として自然か**」で判断する（方針P10）。**Peatix は参照点であって正解ではない**
3. `検討記録.md` 1章の改善①〜⑧は**意図的に Peatix を上回る設計**の箇所。Peatix に合わせてはならない
4. 「D-12 に伴う追記事項」は `仕様書.md` へ反映済みか随時確認する
5. 「試験書へ流用する項目」は **T4-4（試験書作成）まで本シートに残す**。アーカイブしない
