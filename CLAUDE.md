# イベント主催・管理サービス — プロジェクト規約

Peatix で運用している自社イベントの主催・公開・参加者管理を内製化する課題。
**Peatix と「機能が同じ」水準（機能パリティ）を単一 HTML ファイルで実現できるかの検証**が目的であり、
UI デザインの模倣は目的ではない。

- 期間: 2026/07/27(月) 〜 2026/07/31(金)
- 成果物: **単一ファイル `index.html`** ＋ 各種ドキュメント
- 実行前提: **ファイルをダブルクリックし `file://` で開いて動作すること**

---

## 1. 絶対制約（違反したコードは動作しない・要件を満たさない）

`file://` で開く単一ファイルという前提により、以下は **すべて禁止**。
`http://localhost` では動いてしまうため、ローカルサーバで確認しても検証にならない。

| # | 禁止事項 | 理由 |
|---|---|---|
| 1 | `<script type="module">` / `import` / `export` | `file://` では CORS により読み込みが失敗する。**classic script のみ**使用する |
| 2 | **ファイル分割**（`.js` / `.css` / 画像ファイルの外出し） | 成果物は `index.html` 1ファイルのみ。`<style>` と `<script>` はすべて `index.html` 内にインライン記述する |
| 3 | **ビルドツール・バンドラー**（webpack / vite / npm scripts 等） | 使用しない。ソースがそのまま成果物である状態を保つ |
| 4 | **外部ライブラリ・CDN**（React / Vue / Tailwind / QRライブラリ等） | 原則使用しない。**Vanilla JS / CSS のみ**。→ 例外は §2 を参照 |
| 5 | `fetch()` / `XMLHttpRequest` によるローカルファイル読み込み | `file://` では失敗する |
| 6 | Web Worker / Service Worker | `file://` オリジンでは登録できない |
| 7 | 外部画像 URL の参照 | オフライン・外部依存禁止。画像は Blob または data URI |
| 8 | `<canvas>` に外部画像を描画してから `toDataURL()` | オリジン汚染で例外になる |
| 9 | **`localStorage` / `sessionStorage`** | データ永続化は **IndexedDB に一本化**する（§3参照） |

> **#8 の例外**: `<input type="file">` から `FileReader` で読み込んだユーザーアップロード画像は
> 外部リソースではないため、`<canvas>` への描画と `toDataURL()` / `toBlob()` が可能。
> **カバー画像・組織ロゴ・ヘッダー画像のリサイズ（D-15 / D-37 / D-38）はこの方法で実装する。**
> 禁止されるのは外部URLの画像を描画する場合のみ。

---

## 2. 判断に迷ったとき

**ここに書かれていないものを勝手に追加しない。まず提案し、合意を取ってから進める。**

これは特に以下の場面に適用される。

- **外部ライブラリを使いたくなったとき** — 仕様書の要件上どうしても必要な場合（例: Excel出力・PDF出力）のみ検討の余地がある。**追加前に必ず確認を取ること。** 無断で追加してはならない
- 技術選定（データ構造・ライブラリ・アーキテクチャ）で選択肢が複数あるとき
- 制約と要件が両立しないと判明したとき

一方、**仕様の細部が未定義なだけの場合は止まらない** — §5 の既定ルールに従って判断し、先に進む。

---

## 3. 実装方針

### 構成
- `index.html` に `<style>` → マークアップ → `<script>` の順で記述
- ルーティングは `location.hash`（`#/events`, `#/public/:eventId` 等）。History API は `file://` で不安定なため使わない
- 管理者側と参加者側を同一ファイルに同居させ、ハッシュルートで切り替える

### データ永続化 — IndexedDB に一本化
| 用途 | 手段 |
|---|---|
| 業務データ | **IndexedDB** — DB名は `event-manager-db` で固定 |
| UI状態（開いていたタブ・表示設定） | **IndexedDB の `settings` ストア**（localStorage は使用しない） |
| ログイン状態（F-27） | **メモリ上のみ**。ページ再読込で解除される（sessionStorage を使わないため） |

- **DB名を `app` 等の一般名にしない。** `file://` は単一オリジン扱いのため、他のローカル HTML と名前空間を共有してしまう
- **`onupgradeneeded` によるスキーマバージョン管理を初期実装から入れる。** ファイルを差し替えても旧データが残り続けるため
- ストア構成は `仕様書.md` 6章 データモデル に従う（`events` / `entries` / `settings`）
- 券種は `events.ticketTypes[]` として内包し、`entries.ticketTypeId` で参照する（ストアは増やさない）

### スタイリング
- **CSS変数によるデザイントークン管理**。色・余白・角丸・フォントサイズは `:root` に定義し、直値をコンポーネント側に散らさない
- **ライトテーマ（暖色寄りのオフホワイト基調）**。トークンの定義は **`仕様書.md` 9章「デザイントークン」**が唯一の正とし、値をここに複製しない
- アクセントカラーは**黒**。主要アクションのボタンは黒地に白文字とする
- ステータス表示（公開中／公開前／終了、参加確定／キャンセル待ち）にのみ**セマンティックカラー**を使う
- F-09（組織ブランディング／改善③・余力枠）は、ロゴ・ヘッダー画像・紹介文の3点。**テーマカラー設定はv1.14で廃止済みのため、`--color-accent` の実行時上書きは行わない。** アクセントカラーは黒固定
- **レスポンシブ対応は不要。PCブラウザでの表示のみを前提とする**
- 左サイドバー固定＋右コンテンツの2カラム。サイドバー幅はトークンで固定

### 動作環境
- **Chrome のみを動作保証対象**とする（Q4で確定）。他ブラウザは保証範囲外として仕様書に明記
- Safari の `file://` は IndexedDB 制約が強いため、動作しなくても仕様通りとする

---

## 4. 制約により代替実装とする領域

`file://` 単一ファイルでは本来の形で実現できない領域。**黙って省略せず、代替手段を実装した上で、
`仕様書.md` 8章 制約と代替案 に理由と本番構成案を記載すること。**

| 領域 | 扱い |
|---|---|
| アカウント認証 | **範囲内（Q2）。** 主催者はパスコードによる簡易ログイン（F-27）、参加者は受付コード＋メールの照合（F-25）。**実セキュリティは持たない**ことを明記する。複数主催者・権限管理は対象外 |
| メール送信 | **範囲内（Q2）。** 文面自動生成 ＋ `mailto:` での既定メーラー起動 ＋ 送信履歴の記録（F-19）。実SMTP送信・自動一斉配信は不可 |
| 決済・チケット販売 | **スコープ外で確定（Q1）。** 券種は**無料券種のみ**扱い、`price` フィールドも持たない |
| URL による外部共有 | 不可（ファイル配布のみ）。共有リンク生成は文字列生成までのモック |
| QRコード | 外部ライブラリ不可のためスコープ外。**英数字の受付コード**で代替する |

### 認証を実装する際の注意

F-27 は「認証機能が存在すること」を示すためのもので、**セキュリティ機構ではない**。
クライアント内の判定は開発者ツールで回避できる。**強度を上げようとして工数を使わないこと。**
代わりに、その制約を仕様書に明記することで要件に応える。

---

## 5. 仕様が未定義の箇所の扱い

**まず `仕様決定シート.md` を確認する。** 入力仕様（必須／文字数／形式／初期値）、
業務ルール（編集・削除の制限、境界値の扱い）、エラーメッセージの文言は
**D-01〜D-39 として確定済み**。ここに答えがあるものを再発明してはならない。

決定シートにも記載がない場合は、**「自社のイベント運営として自然か」で判断する。**
**Peatix は参照点であって正解ではない。** Peatix を参照するのは、日付の境界や
エラー時の挙動など、どちらを選んでも実質差がない細部に限る。機能の要否は自社運用の観点で決める。

さらに `検討記録.md` 1章 の改善①〜⑧は**意図的に Peatix を上回る設計**にしている箇所であり、
Peatix に合わせてはならない。

**§2（技術選定）とは扱いが異なる。** 仕様の細部は止まらず判断し、技術選定は止まって確認する。

---

## 6. ドキュメントとの整合

- 機能ID `F-xx` / 画面ID `S-xx` は **`仕様書.md`**、タスクID `T-xx` は **`task_manegement.md`**、改善ID `①〜⑧` は **`検討記録.md`** と**1対1で一致させる**
- コード中の主要セクションには対応する機能ID / 画面IDをコメントで記す（例: `// F-12 定員・締切・重複申込チェック`）
- 機能を追加・削除したら **`仕様書.md` の 4章 機能一覧 / 5章 画面一覧 を同時に更新する**。実装と一覧のズレは Day4 の機能凍結（T4-3）で必ず問題になる
- **7/30(木) 12:00 以降は機能追加禁止**（Feature Freeze）。以降は不具合修正のみ

---

## 7. 検証方法

**テストランナーは存在しない。唯一の検証手段は `file://` での目視確認。**

- 変更後は必ずブラウザで `index.html` を開き直して確認する
- ローカルサーバ（`python -m http.server` 等）での確認は**検証として認めない**（制約1〜5を見逃す）
- IndexedDB を伴う変更では、**全データ初期化（F-21）を実行してから**の初回起動も確認する
- 開発時のデータ再現には デモデータ投入（F-21）を使う
- **画像のリサイズを伴う変更では、2MB近い大きな画像と正方形の画像の両方で確認する**（D-15 / D-37 / D-38）
- **カテゴリ管理を変更したら、カテゴリを全削除した状態でイベント作成が拒否されることを確認する**（D-12）

---

## 8. コード規約

### 命名規則
| 対象 | 規則 | 例 |
|---|---|---|
| 関数・変数 | **camelCase** | `createEvent`, `entryList` |
| 定数 | **UPPER_SNAKE_CASE** | `DB_NAME`, `MAX_TICKET_TYPES` |
| 機能ID・画面ID | ドキュメントと同一表記 | `F-12`, `S-04` |

### 記述
- UI 文言・エラーメッセージ・コメントは**日本語**
- 素の JavaScript。フレームワーク的な抽象化は作り込まない（レビュー工数が跳ね返るため）
- DOM 生成は `textContent` を基本とし、ユーザー入力を `innerHTML` に渡さない
- 日時は ISO 8601 文字列で保持し、表示時にフォーマットする
- CSV 出力は UTF-8 BOM 付き（Excel で文字化けさせないため）

### フォームのUI規約

Peatix の入力フォームの構成に倣う。

- **公開時に必須となる項目には赤い `*` を付す**（下書き保存時は必須チェックを行わないため、どれが公開時に必須かを常に示す）
- **入力欄の直下にヘルプ文を置き、入力値がどこに表示されるかを記述する**
  （例: イベント名 →「公開イベントページのタイトルとして表示されます」）
- **プレースホルダには空文字ではなく記入例を入れる**
  （例: 説明文 →「例: 新卒メンバー向けに、業務で使うGitの基本操作を90分で学びます。」）

### エラーメッセージの文言

**方針**: 「〜してください」の依頼形。**項目名を必ず含める。** 原因ではなく次の行動を書く。
全文面は `仕様決定シート.md` E節を参照し、**そこにある文言をそのまま使う**（実装ごとに言い換えない）。

### コミットメッセージ
**変更内容が一文で分かる形式**（日本語可）。

```
イベント作成フォームに券種設定を追加
申込時の定員チェックが締切後に効かない不具合を修正
```

---

## 9. 作業の進め方

- **使用するファイルのみを開き、扱うファイルは最小限に済ませる。** 関係のないファイルを探索的に開かない
- 現時点で本プロジェクトのファイルは以下の6つ。

| ファイル | 役割 |
|---|---|
| `index.html` | 成果物 |
| `仕様書.md` | **実装の正。** 機能一覧・画面一覧・データモデル・入力仕様・デザイントークン |
| `仕様決定シート.md` | 入力仕様と業務ルールの確定内容（D-01〜D-39）。エラーメッセージ文言 |
| `検討記録.md` | 改善マトリクス・カバレッジマップ・確認事項 |
| `task_manegement.md` | 進行管理（WBS・進捗）。**実装時は参照不要** |
| `CLAUDE.md` | 本ファイル |

**実装時に開くのは `仕様書.md` と `仕様決定シート.md`。** `task_manegement.md` は
スケジュール管理用であり、実装の判断材料にしない。
