# イベント主催・管理サービス 仕様書 v1

| 項目 | 内容 |
|---|---|
| サービス名 | **ACTIO（アクティオ）** |
| 対象 | NCT が主催する自社イベントの主催・公開・集客・参加者管理 |
| 成果物 | 単一ファイル `index.html`（HTML/CSS/JS をインライン記述） |
| 実行前提 | ファイルをダブルクリックし `file://` で開いて動作すること |
| 動作保証 | Google Chrome のみ |
| 作成日 | 2026/07/27 |
| 版 | v1 |

**関連文書**: `仕様決定シート.md`（入力仕様・業務ルール・エラーメッセージの確定内容 D-01〜D-39）／`task_manegement.md`（WBS・進行方針・改訂履歴）／`検討記録.md`（改善マトリクス・カバレッジマップ・確認事項）／`CLAUDE.md`（実装規約）

---

# 1. 概要・背景

## 1.1 課題の目的（原文）

> Peatix を利用して、NCTでイベントを主催して集客を行っているが、色々と機能的な面で不便がある。このサービスを社内で作成して、外部向けにイベントページを公開し、集客し、参加者を管理する事を便利に進めていきたい。このシステムで、主催するイベントが便利に管理できるようになる仕組みを実装して欲しいです。

## 1.2 目的から読み取れること

| # | 読み取れること | 設計への反映 |
|---|---|---|
| 1 | **「機能的な面で不便がある」** — 動機は乗り換えではなく**不便の解消** | 検討記録 1章 改善マトリクスが本丸。**パリティは前提条件であって目的ではない** |
| 2 | **「外部向けにイベントページを公開し、集客し」** — 公開と集客が明示されている | 公開イベント一覧（F-26）・検索カテゴリ（F-01）・告知文生成（F-29）は**集客機能として必要**。「Peatixにあるから」ではない |
| 3 | **「参加者を管理する」「便利に管理できる仕組み」** — 管理が2回出てくる | **管理が主軸。** イベント管理・参加者管理・ダッシュボードを厚くし、公開側は必要十分に留める |
| 4 | 対象は **NCT が主催する自社イベント** | 単一主催者前提（F-27）と整合。**マーケットプレイス機能（グループ・フォロワー・抽選等）が不要である根拠**にもなる |

**優先順位の指針**: 管理（イベント管理・参加者管理・ダッシュボード） ＞ 公開・集客（公開ページ・一覧） ＞ 申込 ＞ 基盤

実装対象31機能（対象外3件を除く）のうち **18件（58%）が管理系**であり、目的と機能構成は整合している。

## 1.3 本サービスの位置づけ

本課題の第一の問いは「**Peatix と機能が同じサービスを作成できるか**」の検証である。
成果物は**内製化の実現可能性を検証するプロトタイプ**であり、実運用システムではない。
ただし結合試験仕様書と実施結果報告書を成果物に含むため、**実際に動作するもの**として作成する。

---

# 2. スコープ

## 2.1 含むもの

| 領域 | 範囲 |
|---|---|
| イベント管理 | 作成・編集・削除・複製・シリーズ・下書き／公開切替・一覧検索・ページング・運用ファイル添付（余力） |
| 券種 | 複数券種・券種別定員・販売期間・残席表示閾値。**無料券種のみ** |
| 申込フォーム | 氏名・メールの標準必須取得、カスタム項目の定義（追加・並べ替え・必須設定） |
| 公開・集客 | 公開イベントページ・公開イベント一覧（カテゴリ絞り込み）・公開プレビュー・告知文生成・残席表示 |
| 申込 | 券種選択・入力・確認・完了、定員／締切／重複チェック、受付コード発行、申込照会、キャンセル |
| 参加者管理 | 一覧・検索・ページング・チェックイン・CSV出力・メッセージ送信・参加者情報の編集 |
| ダッシュボード | 申込数・定員充足率・チェックイン率の集計 |
| 通知 | 案内メール文面の自動生成＋`mailto:` 起動＋送信履歴の記録 |
| 認証 | 主催者のパスコードによる簡易ログイン、参加者の受付コード＋メール照合 |
| データ管理 | JSONエクスポート／インポート、全データ初期化、デモデータ投入 |

## 2.2 含まないもの

| 対象 | 扱い |
|---|---|
| **決済・チケット販売・精算** | **スコープ外で確定（Q1）。** モックアップ作成のため不要。券種は**無料のみ**とし `price` フィールドも持たない |
| **アカウント認証（実セキュリティ）** | **機能としては範囲内だが、認証としては成立しない。** クライアント内の判定であり開発者ツールで回避できる。複数主催者・権限管理は対象外 |
| **レスポンシブ表示** | **PCブラウザのみを前提とする**との指示による |
| 申込枚数（複数枚の一括申込） | 1申込＝1名で確定 |
| キャンセル待ちと自動繰り上げ | 定員に達した券種は「満席」として選択不可 |
| キャンセルポリシー | キャンセル可能期限の判定を行わない |
| テーマカラー設定 | 配色はデザイントークンの初期値で固定 |
| 閲覧数・コンバージョン率 | 計測を受け取るサーバが存在しない |
| 当日券・抽選・ライブ配信・定額課金・グループ／フォロワー・アシスタントモード・参加者からのメッセージ受信 | 8章および `機能一覧.md`「検討のうえ対象外としたもの」を参照 |

**判断理由の詳細は `機能一覧.md`「検討のうえ対象外としたもの」に記載する。**

## 2.3 実行前提から生じる絶対制約

`file://` で開く単一HTMLファイルという前提により、以下は実現できない。
`http://localhost` では動作してしまうため、**ローカルサーバでの確認は検証にならない**。

| # | 禁止・不可 | 理由 |
|---|---|---|
| 1 | `<script type="module">` / `import` / `export` | `file://` では CORS により読み込みが失敗する |
| 2 | ファイル分割（`.js` / `.css` / 画像の外出し） | 成果物は `index.html` 1ファイルのみ |
| 3 | ビルドツール・バンドラー | ソースがそのまま成果物である状態を保つ |
| 4 | 外部ライブラリ・CDN | 外部依存禁止。Vanilla JS / CSS のみ |
| 5 | `fetch()` / `XMLHttpRequest` によるローカルファイル読み込み | `file://` では失敗する |
| 6 | Web Worker / Service Worker | `file://` オリジンでは登録できない |
| 7 | 外部画像URLの参照 | 画像は Blob または data URI で保持 |
| 8 | `localStorage` / `sessionStorage` | 永続化は IndexedDB に一本化する |
| 9 | Webフォント | 外部依存となるため。`system-ui` 系のみ使用 |

## 2.4 制約により代替実装とする領域

| 領域 | 本サービスでの扱い |
|---|---|
| **アカウント認証** | 主催者はパスコードによる簡易ログイン（F-27）、参加者は受付コード＋メールの照合（F-25）。**実セキュリティは持たない**（10.2参照） |
| **メール送信** | 文面自動生成 ＋ `mailto:` での既定メーラー起動 ＋ 送信履歴の記録（F-19 / F-28）。**実SMTP送信・自動一斉配信は不可** |
| **URLによる外部共有** | 不可（ファイル配布のみ）。共有リンク生成は文字列生成までのモック |
| **QRコード** | 外部ライブラリ不可のため、**英数字の受付コード**で代替（F-24） |
| **運用資料のグループ共有** | **本番構成では組織メンバー間で共有される前提の機能**。本課題では `file://` 単体で動かすため端末内に閉じ、受け渡しは JSON 書き出しで代替する（F-34） |

**各領域の原因と本番実装での解決策は 8章に記載する。**

---

# 3. 動作環境・前提条件

## 3.1 動作環境

| 項目 | 内容 |
|---|---|
| ブラウザ | **Google Chrome のみを動作保証対象とする** |
| 起動方法 | `index.html` をダブルクリックし `file://` で開く |
| ネットワーク | 不要（完全オフラインで動作する） |
| 画面 | PCブラウザのみ。**スマートフォン・タブレットは対象外** |

**Chrome以外は保証範囲外。** とくに Safari の `file://` は IndexedDB の制約が強く、動作しない可能性がある。

## 3.2 ストレージ動作の検証結果（2026/07/27 実測・確定）

診断用HTMLをダブルクリック起動し、起動回数の累積を測定。**再起動およびファイル移動後も記録が増加し続けることを確認。**

| 項目 | 結果 |
|---|---|
| `file://` からの IndexedDB オープン | 可能 |
| ブラウザ再起動後のデータ永続化 | 保持される |
| ファイルを別ディレクトリへ移動した場合 | 保持される（＝ `file://` が単一オリジンとして扱われている） |
| **採用方式** | **Plan A：IndexedDB** |

## 3.3 この結果から確定した設計上の制約

| # | 制約 | 対応 |
|---|---|---|
| 1 | `file://` 配下の他のローカルHTMLとデータベース名前空間を共有する | DB名は `app` 等の一般名を避け、プロジェクト固有名（`event-manager-db`）とする |
| 2 | ファイルを差し替えても旧データが残り続ける | `onupgradeneeded` によるスキーマバージョン管理を初期実装から組み込む |
| 3 | 試験のたびに前回データが残る | 「全データ初期化」機能（F-21）を**必須**とし、試験前提条件として試験書に明記する |

## 3.4 未検証事項（残リスク）

評価者の実行環境は特定できないため、Chrome以外のブラウザでの挙動は保証範囲外とする。

---

# 4. 機能一覧

**`機能一覧.md` を参照。** 課題の成果物として独立して提出するため、本仕様書からは切り出した。
**唯一の正は `機能一覧.md`** であり、ここに値を複製しない。

収録内容: F-01〜F-35 の一覧（カテゴリ／目的／区分／優先度／改善ID）、F-19・F-28・F-29 の切り分け、
目的別の構成比、優先度別の内訳、検討のうえ対象外としたもの、縮退計画。
**2026/07/28 時点で必須31件はすべて実装済み。対象外は F-14 / F-22 / F-32 / F-33 の4件。**

---

# 5. 画面一覧・画面遷移

**`画面一覧.md` を参照。** 同上の理由で切り出した。**唯一の正は `画面一覧.md`。**

収録内容: S-01〜S-16 の一覧（認証要否／役割／主な遷移先）、認証要否の実装仕様、共通レイアウト、
S-09 のタブ構成、画面遷移、余力機能の未実装時の既定挙動。

---

# 6. データモデル

## 6.1 ストア構成

DB名: **`event-manager-db`**（`file://` は単一オリジン扱いのため、`app` 等の一般名を避ける）

**ストアは `events` / `entries` / `settings` / `files` の4つ。** 券種・フォーム項目・送信履歴はいずれも親レコードに内包し、これ以上ストアを増やさない。

| ストア | keyPath | インデックス |
|---|---|---|
| `events` | `id` | `status`, `startAt`, `seriesId`, `category` |
| `entries` | `id` | `eventId`, `email`, `state`, `code` |
| `settings` | `key` | — |
| `files` | `id` | `createdAt` |

`onupgradeneeded` によるスキーマバージョン管理を初期実装から組み込む。
**ファイルを差し替えても旧データが残り続けるため、バージョン管理は必須である。**

**スキーマバージョンは v1.16 で 1 → 2 へ引き上げた**（`files` ストアの追加）。
`onupgradeneeded` では**既存ストアを作り直さず、不足しているストアだけを追加する**。
既存のイベント・申込データを失わせてはならない。

## 6.2 events

| フィールド | 型 | 説明 | 機能 |
|---|---|---|---|
| `id` | string | 主キー | — |
| `title` | string | イベント名 | F-01 |
| `description` | string | 説明文 | F-01 |
| `format` | `'venue'` \| `'online'` | 開催形式 | F-01 |
| `venue` | string | 会場名・住所（`format='venue'`） | F-01 |
| `onlineUrl` | string | 視聴URL（`format='online'`） | F-01 |
| `category` | string | 検索カテゴリ | F-01 / F-26 |
| `startAt` / `endAt` | ISO8601 | 開催日時 | F-01 |
| `deadline` | ISO8601 | 申込締切 | F-12 |
| `status` | `'draft'` \| `'published'` | 下書き／公開。**「終了」は保持せず `endAt` から導出** | F-03 |
| `seriesId` | string \| null | シリーズ識別子（複製時に継承） | F-05 / F-18 |
| `ticketTypes` | TicketType[] | 券種（下記） | F-23 |
| `formFields` | FormField[] | カスタム申込項目（下記） | F-07 |
| `coverImage` | Blob \| null | カバー画像 | F-01 |
| `staff` | `{ owner:string, members:{ id, name, role }[] }` | 運営人員（責任者・運営メンバーと担当）。**参加者には公開しない** | F-35 |
| `createdAt` / `updatedAt` | ISO8601 | — | — |

**TicketType**: `{ id, name, capacity:number, description, salesStart, salesEnd, lowStockThreshold:number }`
　`lowStockThreshold` は残席表示の閾値。**残席がこの値以下のときのみ「残り○枚」と表示**する（F-31）。

**FormField**: `{ id, label, type:'text'|'textarea'|'select'|'radio'|'checkbox', options:string[], required:boolean, order:number }`

> **氏名・メールは `formFields` に含めない。** 標準必須項目として実装側で固定する（F-06／改善①）。
> 設定漏れを構造的に起こさないという主張を、**データ構造のレベルで担保する**。

## 6.3 entries

| フィールド | 型 | 説明 | 機能 |
|---|---|---|---|
| `id` | string | 主キー | — |
| `eventId` | string | 対象イベント | — |
| `ticketTypeId` | string | 申し込んだ券種 | F-23 |
| `code` | string | 受付コード。**イベント内で一意** | F-24 / F-25 |
| `name` / `email` | string | 標準必須項目 | F-06 |
| `answers` | `{ [fieldId]: value }` | カスタム項目の回答 | F-07 |
| `state` | `'confirmed'` \| `'cancelled'` | 申込状態。**キャンセル待ちは対象外のため `waitlist` は持たない**（F-14 廃止） | F-12 / F-13 |
| `checkedInAt` | ISO8601 \| null | 受付時刻。null は未チェックイン | F-16 |
| `cancelledAt` | ISO8601 \| null | 取消時刻 | F-13 |
| `createdAt` / `updatedAt` | ISO8601 | — | — |

## 6.4 settings（key-value）

| key | 値 | 機能 |
|---|---|---|
| `org` | `{ name:string, logo:Blob\|null, headerImage:Blob\|null }` | F-09（**テーマカラーと紹介文は廃止**） |
| `categories` | `string[]`（**上限20件**） | F-01 / F-26 |
| `defaultFormFields` | FormField[] | F-07 |
| `mailTemplates` | `{ invite, reminder, announce }` | F-19 / F-29 |
| `sendLog` | `{ id, sentAt, eventId, kind, subject, recipientCount }[]` | **F-19 送信履歴** |
| `passcode` | string | F-27。**平文で保持する**（10.2参照） |
| `participants` | `{ [メールアドレス小文字]: string }` | **F-25 参加者のマイチケット用パスワード（D-43）。** `passcode` と同じく平文で保持する（10.2参照） |
| `uiState` | `{ lastTab, ... }` | localStorage を使わないための代替 |
| `schemaVersion` | number | `onupgradeneeded` の判定 |

**`categories` の初期値**（初回起動時に投入。空リストではイベントを作成できないため必須）

```
["セミナー", "勉強会", "研修", "説明会", "懇親会", "その他"]
```

## 6.5 files（F-34 運用資料）

**イベント単位ではなく組織全体の資料置き場**である（S-16）。

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | string | 主キー |
| `eventId` | string \| null | 紐付け先のイベント。**null は組織共通**（特定のイベントに属さない資料） |
| `name` | string | ファイル名（100文字以内） |
| `note` | string | 説明（200文字以内） |
| `mime` | string | MIME タイプ |
| `size` | number | バイト数 |
| `blob` | Blob | ファイル本体。**リサイズは行わず原本を保持する** |
| `createdAt` | ISO8601 | 登録日時 |

> カバー画像・組織ロゴ・ヘッダー画像（D-15 / D-37 / D-38）と異なり、
> 運用資料は原本を保持する必要があるためリサイズしない。

## 6.6 保持せず導出する値

**保存すると実データとの整合が崩れるため、都度計算する。**

| 導出値 | 計算方法 | 機能 |
|---|---|---|
| イベントの「終了」 | `endAt < 現在時刻` | F-03 / S-10 |
| 券種ごとの残席 | `ticketType.capacity − 当該券種の state='confirmed' 件数`。**`lowStockThreshold` 以下のときのみ公開側に表示** | F-31 |
| 定員充足率 | 上記の集計 | F-18 |
| チェックイン率 | `checkedInAt !== null` の件数 ÷ `confirmed` 件数 | F-18 |

## 6.7 判定ルール

- **定員は「イベント全体」ではなく券種単位**で判定する（F-12 / F-23）
- **重複申込の判定は `eventId` ＋ `email`。** `state='cancelled'` は対象外とし、再申込を許可する
- **定員に達した券種は「満席」として選択不可**とし、申込を受け付けない（F-14 廃止によりキャンセル待ちは存在しない）
- **使用中のカテゴリは削除できない。** 当該カテゴリを持つイベントが1件以上ある場合、削除を拒否する（D-03 と同じ方針で統一）
- **カテゴリの登録上限は20件**（D-12）
- **JSONエクスポート（F-20）では `coverImage` / `logo` / `headerImage` / `files` の Blob を base64 に変換する。** Blob はそのままJSON化できないため
- **`files` ストアはエクスポート時に「資料ファイルを含める」チェックボックスで制御し、既定はオフとする。** 5MB × 20件で100MB超となり `JSON.stringify` が破綻するため

## 6.8 永続化に関する制約

- **`localStorage` / `sessionStorage` は使用しない。** UI状態も `settings.uiState` に保持する
- **ログイン状態（F-27）はメモリ上のみ。** ページ再読込で解除される
- `onupgradeneeded` によるスキーマバージョン管理を初期実装から組み込む（`schemaVersion = 1` から開始）

---

# 7. 入力仕様

**定義の実体は `仕様決定シート.md` にある。** 入力仕様（必須／文字数／形式／初期値）・業務ルール・
エラーメッセージ文言の**唯一の正はシート側**であり、本章は D-ID から引くための索引に留める。
二重管理を避けるため、**値を本章に複製しない。**

## 7.1 業務ルール（シート A節）

| ID | 論点 | 対応 |
|---|---|---|
| D-01 | 下書き保存時の必須チェック | F-03 / S-03 |
| D-02 | 申込者がいる券種の定員縮小 | F-23 / S-03 |
| D-03 | 申込者がいる券種の削除 | F-23 / S-03 |
| D-04 | 申込者がいるイベントの削除 | F-02 / S-02 |
| D-05 | 券種ゼロでの公開 | F-03 / F-23 |
| D-06 | 申込締切の境界 | F-12 |
| D-07 | 終了判定の境界 | F-03 / S-10 |
| D-08 | 下書きイベントへの直接アクセス | F-03 / S-14 |
| D-09 | キャンセル後の再申込 | F-12 / F-13 |
| D-10 | チェックイン済み参加者のキャンセル | F-13 / F-16 |

## 7.2 イベント作成／編集（S-03・シート B節）

| ID | 項目 | ID | 項目 |
|---|---|---|---|
| D-11 | イベント名 | D-21 | 券種名 |
| D-12 | カテゴリ | D-22 | 定員 |
| D-13 | 開始日時 | D-23 | 券種の説明 |
| D-14 | 終了日時 | D-24 | 販売開始／終了 |
| D-15 | カバー画像 | D-25 | 残席表示の閾値（F-31） |
| D-16 | 開催形式 | D-26 | 申込締切 |
| D-17 | 会場名・住所 | D-28 | カスタム項目 |
| D-18 | 視聴URL | D-29 | 公開状態 |
| D-19 | 説明文 | | |

**カバー画像（D-15）は表示方針を伴う。** 公開ページのヘッダー（横長）と一覧カード（16:9程度）で
比率が異なるため、**保存は元の比率を維持し、表示側で `object-fit: cover` の中央トリミングを行う**。
詳細はシート B-1 を参照。

## 7.3 申込フォーム（S-06・シート C節）

| ID | 項目 |
|---|---|
| D-30 | 氏名 |
| D-31 | メールアドレス |
| D-43 | マイチケット用パスワード（S-11 のログインに使う。4〜16文字の英数字） |

カスタム項目の必須・形式は `events.formFields` の定義に従う（6.2参照）。

## 7.4 その他の値（シート D節）

| ID | 論点 | 対応 |
|---|---|---|
| D-32 | 受付コードの形式 | F-24 / F-25 |
| D-33 | パスコードの形式 | F-27 |
| D-35 | 添付ファイル | F-34（D-40 に統合） |
| D-36 | 一覧の既定の並び順 | F-04 / F-15 |

## 7.5 組織ブランディング（S-09「一般設定」タブ・シート D'節）

| ID | 項目 |
|---|---|
| D-37 | 組織ロゴ |
| D-38 | ヘッダー画像 |

実装の優先順位は **ロゴ ＞ ヘッダー画像 ＞ 紹介文**。F-09 は余力枠のため、未実装時の既定挙動は `画面一覧.md` に定義する。

## 7.6 運用資料（S-16・シート D''節）

| ID | 項目 |
|---|---|
| D-40 | 資料ファイル（`pdf, png, jpeg, xlsx, docx`／1ファイル5MB以下／登録上限20件） |
| D-45 | 資料の紐付け先イベント（選択必須。「組織共通」も選べる） |
| D-41 | 資料の説明 |
| D-42 | 既定の申込フォーム項目（参加のきっかけ／懇親会に参加する／質問・要望） |
| D-44 | 運営人員（責任者／運営メンバーの氏名・担当。各50文字） |

## 7.7 エラーメッセージ（シート E節）

**方針**: 「〜してください」の依頼形。**項目名を必ず含める。** 原因ではなく次の行動を書く。
**全文面はシート E節にあり、実装時はそこの文言をそのまま使う**（実装ごとに言い換えない）。

## 7.8 欠番

D-20（キャンセル規定）・D-27（キャンセル期限）は **F-32 廃止**、D-34（待機番号の採番）は **F-14 廃止**により削除した。
追跡性を保つため再採番はしない。

---

# 8. 制約と代替案

**「単一 `.html` を `file://` で開いて動作する」という実行前提から生じる構造的な限界。** 機能設計の問題ではなく実行形態の問題であるため、本番実装での解決策とセットで示す。

| # | 実現できないこと | 原因 | 本課題での代替 | 本番実装での解決策 |
|---|---|---|---|---|
| 1 | 決済・チケット販売・精算 | 決済代行APIとの通信、および秘匿情報を扱うサーバが必要。クライアントのみでは決済情報を安全に扱えない | **【Q1により対象外と確定】** 券種は**無料券種のみ**扱い、`price` フィールドも持たない | 決済代行（Stripe等）＋サーバサイドでのWebhook受信。改善⑦の手数料削減はここで実現する |
| 2 | 実際のメール送信（案内・リマインド） | ブラウザ単体でSMTP送信は不可能。`mailto:` は本文長・宛先数に制限がある | **【Q2により範囲内】** 文面を自動生成し、`mailto:` で既定メーラーを起動（宛先・件名・本文を差し込み済み）。送信履歴を記録。**自動送信・大量一斉配信は不可** | サーバ経由でのメール配信サービス（SES / SendGrid等）連携 |
| 2b | **申込完了時に参加者へ自動でメールが届かない** | #2と同じ。自動送信の契機を持てない | **受付コードは申込完了画面（S-07）に表示し、参加者に控えてもらう。** 主催者が F-28 で後追い送信する運用とする | 申込完了をトリガーにサーバから自動送信 |
| 3 | URL共有による外部からの参加申込 | `file://` は他者からアクセスできない。共有には**ファイルそのものの配布**が必要で、その場合データも共有されない | 共有リンク生成は**文字列生成までのモック**。申込動作は同一ファイル内で検証する | Webサーバへのホスティング。ここが解消されると他の制約もほぼ同時に解消する |
| 4 | アカウント認証・複数主催者・権限管理 | 認証情報を検証する主体が存在しない。クライアント内の判定は開発者ツールで容易に回避できる | **【Q2により範囲内】** 主催者はパスコードによる簡易ログイン（F-27）、参加者は受付コード＋メールアドレスの照合（F-25）。**いずれも実セキュリティは持たず、機能の存在と操作導線を示すもの**。複数主催者・権限管理は対象外 | サーバサイド認証（セッション／OIDC）＋ロールベースの権限管理 |
| 5 | QRコード付き電子チケット | 外部ライブラリの読み込みが禁止されており、QRエンコーダを自前実装するのは工数に見合わない | **英数字の受付コード**を発行。受付側はコード入力または参加者一覧からのタップで受付 | QR生成ライブラリの導入。受付アプリ側でカメラ読み取り |
| 6 | データの複数端末間共有・同時編集 | データが端末内の IndexedDB に閉じている | **JSONエクスポート／インポート**による手動同期（F-20） | サーバサイドDBへの集約 |
| 7 | 閲覧数・流入元などのアクセス解析 | 計測を受け取るサーバがない | 申込データから算出できる指標（申込数・充足率・推移）のみダッシュボードで提供 | 解析基盤の導入 |
| 8 | ライブ配信（Peatix Live 相当） | 動画のエンコード・配信・視聴者管理を行う基盤が必要 | 実装しない。**オンラインイベントは「開催形式＝オンライン」＋視聴URLの案内**までを扱う（F-01） | 配信基盤（Zoom / YouTube Live 等）との連携、または専用の配信サービス |
| 9 | **複数の主催者（他社・個人）による利用** | データが各PCの IndexedDB に閉じており、主催者間・参加者間でデータを共有できない | 実装しない。単一組織での運用を前提とする | 共有データベースと API サーバが必須 |
| 10 | **参加者・主催者のアカウント登録とロール分離** | 同上。認証情報を検証する主体が存在しない | 主催者はパスコード（F-27）、参加者は**メールアドレス＋パスワード**（D-43）で代替。**いずれも端末内の平文照合であり、パスワード再発行もできない。アカウントとして成立しているわけではない** | 認証基盤の導入（ハッシュ化・再発行・セッション管理） |
| 11 | **主催者グループの作成** | マルチテナント構成を前提とする機能 | 単一組織での運用では名前空間が不要となり、階層自体が不要（検討記録 5.2） | 主催者テーブルによる名前空間の導入 |
| 12 | **主催者を横断した公開イベント一覧** | 他主催者のデータを参照できない | 自組織のイベントのみを一覧する（F-26） | 共有データベース上での横断検索 |

## 8.1 この評価から導かれること

`file://` 単一ファイルという制約下でも、**イベント運営業務そのもの（企画・公開・集客受付・当日運営・名簿管理）は一通り成立する**。

対象外とした領域および代替が必要になった領域は、いずれも**サーバサイド基盤の有無に起因**しており、機能設計上の限界ではない。**Webサーバ上にホスティングし、認証・決済・メール配信を組み合わせれば、いずれも標準的な手段で解決できる**。したがって本課題の成果物は、**内製化の実現可能性を検証するプロトタイプとして機能する**。

**特に注意すべき点**: 認証（#4）は「機能としては動くが、セキュリティとしては成立しない」ものである。デモとしては要件を満たすが、**この実装のまま本番運用してはならない**（10.2参照）。

## 8.2 本番実装での構成案

API サーバ ＋ RDB（主催者・イベント・申込を別テーブルで管理）＋ 認証基盤（主催者／参加者のロール分離）。
**主催者テーブルを設けることでマルチテナント構成が成立し、グループ機能・横断検索・アカウント登録がいずれも実現可能となる。**
本課題の成果物は、この構成における**画面と業務ロジックの検証**に相当する。

---

# 9. デザイントークン

## 9.1 方針

- **ライトテーマ固定・白基調**（v1.18）。面はすべて白で統一し、**階層は罫線で切る**。
  管理画面のコンテンツ地のみ `--color-canvas`（ほぼ白）でわずかに沈め、カードとの前後関係を出す
- **アクセントカラーは黒**。主要アクション（イベントを作成／詳細を見る／メッセージを送信 等）は黒地に白文字
- **色で意味を持たせるのはステータス表示のみ**。それ以外は無彩色で構成し、情報量の多い一覧画面でも視線が散らないようにする
- 値は必ず `:root` の CSS変数から参照する。**コンポーネント側に直値を書かない**
- **本章がトークンの唯一の定義元**。CLAUDE.md・実装コードは本章を参照する
- **v1.17 で参照デザイン（Issue #2 画像2）のトーンに寄せた。** ミニマルでエディトリアルな SaaS ダッシュボードを狙い、
  影を外して細い罫線で面を切り、余白と角丸を広げ、字間を広げた英字ラベルで小見出しを立てる

## 9.2 カラー

### 基本

| トークン | 値 | 用途 |
|---|---|---|
| `--color-bg` | `#FFFFFF` | ページ背景 |
| `--color-surface` | `#FFFFFF` | カード・テーブル・モーダル・サイドバー・ヘッダー |
| `--color-surface-sub` | `#F4F5F7` | ホバー、選択中のナビ項目、プレースホルダ面 |
| `--color-canvas` | `#FAFAFB` | 管理画面のコンテンツ地。白との差はごく薄く |
| `--color-border` | `#E5E7EB` | 境界線・区切り線・入力欄の枠 |
| `--color-text` | `#16181D` | 本文・見出し |
| `--color-text-muted` | `#6B7280` | 補足テキスト・ラベル・プレースホルダ |

### アクセント

| トークン | 値 | 用途 |
|---|---|---|
| `--color-accent` | `#16181D` | 主要ボタンの背景、アクティブ要素 |
| `--color-accent-text` | `#FFFFFF` | 主要ボタンの文字色 |

### サービスロゴ（Issue #2 画像1）

ロゴマークは**外部画像を使わず SVG で描画する**（制約7）。3つのポリゴンで峰を構成する。

| トークン | 値 | 用途 |
|---|---|---|
| `--color-mark-facet` | `#2C3440` | マーク内側のファセット |
| `--color-mark-accent` | `#8DA5C4` | マーク右面（スチールブルー） |

濃色の主面には `--color-accent` を使う。ワードマーク「ACTIO」は字間 `.22em`。

> **テーマカラーの設定機能は廃止した。** `--color-accent` は黒で固定する。
> F-09 は**自社ロゴの登録のみ**となり、配色は変更できない。
> 将来カラー設定を追加する場合は、`--color-accent` を実行時に上書きする形（`setProperty`）で拡張できる。

### セマンティック（ステータス表示専用）

| トークン | 背景 | 文字 | 用途 |
|---|---|---|---|
| `--color-success-*` | `#E4F1E5` | `#2E7D40` | 公開中、参加確定 |
| `--color-warning-*` | `#FBF1DA` | `#96792A` | 公開前、残りわずか |
| `--color-neutral-*` | `#F1F2F4` | `#5B616E` | 終了、満席 |
| `--color-danger-*` | `#FBE6E3` | `#B23B2C` | キャンセル済み、削除、エラー |

### データ可視化

| トークン | 値 | 用途 |
|---|---|---|
| `--color-progress` | `#4CAF6D` | 定員充足率のプログレスバー（F-18） |
| `--color-chart-line` | `#4A7BC8` | 申込推移の折れ線（F-18） |
| `--color-chart-grid` | `#EDEEF1` | グラフのグリッド線 |

## 9.3 余白

4px を基準単位とする。

| トークン | 値 |
|---|---|
| `--space-xs` | `4px` |
| `--space-sm` | `8px` |
| `--space-md` | `12px` |
| `--space-lg` | `16px` |
| `--space-xl` | `24px` |
| `--space-2xl` | `32px` |
| `--space-3xl` | `48px` |

## 9.4 角丸

| トークン | 値 | 用途 |
|---|---|---|
| `--radius-sm` | `4px` | 入力欄、ボタン、バッジ、サムネイル |
| `--radius-md` | `6px` | モーダル、サイドバーの項目 |
| `--radius-lg` | `10px` | カード、数値タイル |
| `--radius-full` | `9999px` | アバター、ステップ表示、チップ |

## 9.5 タイポグラフィ

| トークン | 値 | 用途 |
|---|---|---|
| `--font-family` | `system-ui, "Hiragino Sans", "Noto Sans JP", sans-serif` | 全体（**Webフォントは使用しない**） |
| `--font-size-xs` | `12px` | バッジ、注釈 |
| `--font-size-sm` | `13px` | テーブル本文、ラベル |
| `--font-size-md` | `14px` | 本文（基準） |
| `--font-size-lg` | `16px` | カード見出し |
| `--font-size-xl` | `20px` | 画面タイトル |
| `--font-size-2xl` | `28px` | 数値の強調表示（申込数など） |
| `--font-size-display` | `40px` | 公開ページのイベント名 |
| `--font-weight-normal` | `400` | 本文 |
| `--font-weight-medium` | `500` | ラベル、見出し |
| `--font-weight-bold` | `700` | 画面タイトル、数値強調 |
| `--line-height` | `1.7` | 本文（日本語のため広め） |

## 9.6 境界・影

| トークン | 値 | 用途 |
|---|---|---|
| `--border` | `1px solid var(--color-border)` | 標準の境界線 |
| `--shadow-sm` | `0 1px 2px rgba(22,24,29,.05)` | 予備（**常用しない。罫線のみで面を切る**） |
| `--shadow-md` | `0 6px 20px rgba(22,24,29,.08)` | モーダル、ドロップダウン、ホバー時のカード |

## 9.7 レイアウト

| トークン | 値 | 用途 |
|---|---|---|
| `--sidebar-width` | `200px` | 左サイドバー（固定） |
| `--content-max-width` | `1200px` | コンテンツ領域の最大幅 |
| `--header-height` | `56px` | 画面上部の見出し行 |

**構成**: 左サイドバー固定 ＋ 右コンテンツの2カラム。**レスポンシブ対応は行わない**（PCブラウザのみ）。

**管理画面のコンテンツは画面幅いっぱいに広げる。** `--content-max-width` で中央寄せすると、
広い画面で右側に大きな余白ができてしまうため（Issue #4）。
**参加者側はヘッダー画像のみ全幅**（角丸なし）とし、それ以外のセクションは `--content-max-width` で
中央寄せして左右に余白を取る。

**サイドバーは白（`--color-surface`）**とし、右コンテンツの背景（`--color-bg`）との差で領域を分ける。
アクティブ項目は `--color-surface-sub` のピルで示し、アクセント色で塗り潰さない。

**管理側・参加者側ともに、同じ高さ・同じロゴ位置・同じ字送りのヘッダー（`--header-height`）を持つ。**
サイドバーの有無は変えつつ、ヘッダーとカードの意匠を共通化して1つのサービスとして見せる。

## 9.8 実装形

```css
:root {
  /* 基本 — 白基調 */
  --color-bg:            #FFFFFF;
  --color-surface:       #FFFFFF;
  --color-surface-sub:   #F4F5F7;
  --color-canvas:        #FAFAFB;
  --color-border:        #E5E7EB;
  --color-text:          #16181D;
  --color-text-muted:    #6B7280;

  /* アクセント */
  --color-accent:        #16181D;
  --color-accent-text:   #FFFFFF;

  /* ロゴマーク */
  --color-mark-facet:    #2C3440;
  --color-mark-accent:   #8DA5C4;

  /* セマンティック */
  --color-success-bg:    #E4F1E5;  --color-success-text: #2E7D40;
  --color-warning-bg:    #FBF1DA;  --color-warning-text: #96792A;
  --color-neutral-bg:    #F1F2F4;  --color-neutral-text: #5B616E;
  --color-danger-bg:     #FBE6E3;  --color-danger-text:  #B23B2C;

  /* 可視化 */
  --color-progress:      #4CAF6D;
  --color-chart-line:    #4A7BC8;
  --color-chart-grid:    #EDEEF1;

  /* 余白 */
  --space-xs: 4px;   --space-sm: 8px;   --space-md: 12px;
  --space-lg: 16px;  --space-xl: 24px;  --space-2xl: 32px; --space-3xl: 48px;

  /* 角丸 */
  --radius-sm: 4px;  --radius-md: 6px;  --radius-lg: 10px;  --radius-full: 9999px;

  /* タイポグラフィ */
  --font-family: system-ui, "Hiragino Sans", "Noto Sans JP", sans-serif;
  --font-size-xs: 12px;  --font-size-sm: 13px;  --font-size-md: 14px;
  --font-size-lg: 16px;  --font-size-xl: 20px;  --font-size-2xl: 28px;
  --font-size-display: 40px;
  --font-weight-normal: 400; --font-weight-medium: 500; --font-weight-bold: 700;
  --line-height: 1.7;

  /* 境界・影 */
  --border:    1px solid var(--color-border);
  --shadow-sm: 0 1px 2px rgba(22,24,29,.05);
  --shadow-md: 0 6px 20px rgba(22,24,29,.08);

  /* レイアウト */
  --sidebar-width: 200px;
  --content-max-width: 1200px;
  --header-height: 56px;
}
```

## 9.9 ステータス表示の対応

| 表示 | トークン | 対応機能 |
|---|---|---|
| 公開中 | `success` | F-03 |
| 公開前（下書き） | `warning` | F-03 |
| 終了 | `neutral` | F-03（`endAt` から導出） |
| 申し込み済み（未受付） | `warning` | F-12 |
| 参加確定（受付済み） | `success` | F-16 |
| 満席 | `neutral` | F-12 |
| 残りわずか | `warning` | F-31（閾値以下のとき） |
| キャンセル済み | `danger` | F-13 |

## 9.10 参照デザインから取り入れた画面パーツ

| パーツ | 内容 |
|---|---|
| **アイブロウ（`.eyebrow`）** | 「FEATURED EVENT」のような字間 `.18em` の英字小見出し。セクションの性格を示す |
| **数値タイル（`.tile`）** | ダッシュボードの主要指標。ラベル（小・字間広め）＋大きな数値＋注記。必要に応じて充足率バーを併置 |
| **縦棒グラフ（`barChart`）** | シリーズ横断の動員推移。左端が第1回。下書きの回はグレーで示す |
| **行サムネイル（`.rowthumb`）** | 一覧行の先頭に 56×36 のカバー画像。未設定時はカテゴリ頭文字 |
| **イベントカード（`.eventcard`）** | 公開イベント一覧。左に 16:9 のカバー画像、右に日時・タイトル・場所・カテゴリ |
| **ステップ表示（`.steps`）** | S-03 の5ステップ。現在地は塗り、通過済みは濃い文字色 |

| **参加者トップのヒーロー（`.phero`）** | ヘッダー画像に暗いオーバーレイを敷き、キャッチコピー・リード・**検索バー**（キーワード／開催形式／カテゴリ／開催日以降）を重ねる。画像未設定時はグラデーションで代替 |
| **イベントカード（`.ecard`）** | 参加者トップの横並び4カラム。上に 16:9 の画像、**画像左上にカテゴリバッジ**、下にイベント名・日時・場所・定員 |
| **カテゴリチップ（`.catchip`）** | 「カテゴリから探す」。件数つきで、選択中は枠をアクセント色に |
| **管理画面フッター（`.appfoot`）** | 組織名とサービス名、データの保持場所の注記 |

> **閲覧数・コンバージョン率のタイルは作らない。** 参照デザインには含まれるが、計測サーバがないため取得できない（8章 #7）。

### 画像を含むレイアウトの注意

**画像を含む2カラムはフレックスではなくグリッドで組む。** 画像を `flex` 項目にすると、
置換要素の自動最小サイズ（intrinsic size）が下限になって縮まず、**本文カラムが潰れて1文字ずつ折り返す**
（Issue #3 で発生）。`grid-template-columns: minmax(0,1fr) minmax(0,42%)` と高さの固定で回避する。

> **ダッシュボードの注目イベント（ヒーロー）は Issue #5 で廃止した。** 「ダッシュボードに注目イベントがあるのは違和感がある」との指摘による。

## 9.11 充足率バーの配色

トラックは **`--color-border`** を使う。`--color-surface-sub` にすると**テーブル行のホバー色と同じ**になり、
ホバー時にバーが背景へ溶けて見えなくなる（Issue #1 で報告）。

---

# 10. 非機能要件

## 10.1 性能

| 項目 | 想定 |
|---|---|
| 同時に扱うイベント数 | 数十件 |
| 1イベントあたりの参加者数 | 数百名 |
| 一覧表示 | **ページングを実装**（F-04 / F-15） |

## 10.2 セキュリティ（重要）

**本システムの認証はセキュリティ機構ではない。**

| 項目 | 実態 |
|---|---|
| 主催者ログイン（F-27） | クライアント内でパスコードを照合するのみ。**開発者ツールで容易に回避できる** |
| パスコードの保持 | **平文**。クライアント内判定であり、ハッシュ化しても秘匿の意味を持たないため |
| 参加者の本人確認（F-25） | 受付コード＋メールアドレスの照合のみ |
| 通信の保護 | 通信そのものが発生しない |

> **この実装のまま本番運用してはならない。**
> 認証機能が「存在すること」を示すための実装であり、認証として機能させるにはサーバサイドの検証が必須である（8章 #4 参照）。

## 10.3 データ保全

| 項目 | 内容 |
|---|---|
| 保存場所 | 端末内の IndexedDB のみ |
| **消失リスク** | **ブラウザの閲覧データ削除で全消失する** |
| バックアップ手段 | **JSONエクスポート（F-20）が唯一の手段**。定期的な実行を運用で担保する |
| 複数端末での共有 | **不可。** JSONエクスポート／インポートによる手動同期のみ |

## 10.4 その他

- UI文言・エラーメッセージは日本語
- CSV出力は **UTF-8 BOM付き**（Excelでの文字化けを防ぐため）
- 日時は ISO 8601 で保持し、表示時にフォーマットする
- ユーザー入力を `innerHTML` に渡さない（DOM生成は `textContent` を基本とする）
