# イベント主催・管理サービス 画面一覧

**サービス名**: ACTIO（アクティオ）
**版**: v1.19（2026/07/28）
**位置づけ**: 課題の成果物として独立して提出する画面一覧。**本ファイルが唯一の正**であり、`仕様書.md` には複製しない。

**関連文書**: `仕様書.md`（スコープ・データモデル・入力仕様・制約・デザイントークン・非機能要件）／`仕様決定シート.md`（入力仕様と業務ルールの確定値 D-01〜D-44）／`検討記録.md`（判断の経緯）／`task_manegement.md`（進行管理）

---

## 1. 画面一覧

| 画面ID | 画面名 | 認証 | 役割 | 主な遷移先 |
|---|---|---|---|---|
| S-01 | ダッシュボード | **要** | 全体状況の把握 | S-02, S-04, **S-03, S-08, S-09** |
| S-02 | イベント一覧 | **要** | 検索・複製・状態確認・**ページング** | S-03, S-04 |
| S-03 | イベント作成／編集 | **要** | **5ステップのウィザード**（1 基本＝イベント名・開催形式／2 会場・配信／3 詳細＝日時・カテゴリ・カバー画像・説明文／4 チケット／5 その他＝カスタム項目・運営人員・公開状態）。ステップは直接クリックでも移動でき、入力値は保持される | S-04 |
| S-04 | イベント詳細（管理） | **要** | 申込状況と参加者への導線・告知文の生成（F-29）・共有リンクの生成（F-10） | **S-05(プレビュー), S-08, S-03** |
| S-05 | 公開イベントページ | 不要 | 外部向け表示・申込導線 | S-06, **S-10, S-11** |
| S-06 | 申込フォーム | 不要 | 券種選択＋参加者情報入力 | **S-13, S-05** |
| S-07 | 申込完了 | 不要 | 受付コードの表示 | S-05, S-11, **S-10** |
| S-08 | 参加者一覧／受付 | **要** | 検索・**ページング**・チェックイン・CSV出力・メッセージ送信（宛先選択）・参加者情報の編集・キャンセル処理（主催者側・F-13） | S-04 |
| S-09 | 設定・データ管理 | **要** | **タブ構成（5.3参照）** | S-01 |
| S-10 | 公開イベント一覧 | 不要 | 公開中イベントの一覧・検索（**参加者側の唯一の入口**）。**終了イベントは除外**。構成は上から ①ヒーロー（ヘッダー画像＋キャッチ＋検索バー）②公開中のイベント（横並びカード）③カテゴリから探す | S-05, **S-11** |
| S-11 | **マイチケット** | 不要 | **メールアドレス＋パスワードでログイン**し、**同一メールアドレスの申込を全件表示**。各申込の内容確認とキャンセルが可能。終了済みイベントの申込も履歴として表示する（キャンセル操作は不可） | S-05, S-10 |
| S-12 | 主催者ログイン | — | 管理画面へのアクセス制御（パスコード入力）・初回起動時のパスコード初期設定・**未認証で要認証の画面にアクセスした際のリダイレクト先** | S-01 |
| S-13 | 申込内容確認 | 不要 | 申込前の入力内容確認 | S-07, **S-06** |
| S-14 | エラー | 不要 | 存在しないイベントID・不正なハッシュルートを指定された場合の表示 | S-10 |
| **S-16** | **資料** | **要** | **組織の運用資料の登録・一覧・ダウンロード・削除。イベント単位ではなく組織全体の資料置き場（F-34）** | S-01 |

> **S-11 でメールアドレスのみでの一覧表示は行わない。** 他人のメールアドレスを入力すれば申込内容を閲覧できてしまうため、**必ず2要素で照合する。**
> **v1.17 で2要素目を受付コードからパスワード（D-43）へ変更した。** 受付コードは申込ごとに異なるため、
> 「同一メールの申込を全件見る」という要件と両立しなかったのが理由である。
> パスワードは申込フォームで設定し、同じメールアドレスの2回目以降は登録済みのものと照合する。
> **F-27 と同様、端末内での平文照合であり実セキュリティは持たない**（10.2参照）。
> 照会失敗時は「メールアドレスまたはパスワードが正しくありません」とし、**登録の有無を区別しない。**

> S-13 は追番のため一覧上は末尾に位置するが、**遷移順は S-06 と S-07 の間**である。既存IDの追跡性を保つため再採番はしない。

> **S-15（管理側マイチケット）は削除した。** 全券種の集計表であり S-01 ダッシュボードと内容が重複していたため、券種の状況は S-01 と S-04 で確認する（検討記録 5.1 #1）。

## 2. 認証要否（ルーティングガードの実装仕様）

| 認証 | 画面 |
|---|---|
| **要** | S-01, S-02, S-03, S-04, S-08, S-09, **S-16** |
| 不要 | S-05, S-06, S-07, S-10, S-11, S-13, S-14 |
| — | S-12（ログイン画面自身） |

**未認証で「要」の画面にアクセスした場合は S-12 へリダイレクトする。** 結合試験の項目に直結する。

## 3. 共通レイアウト

### 管理画面のヘッダー（全管理画面に固定表示）

| 位置 | 内容 |
|---|---|
| 左 | 組織ロゴ（`settings.org.logo`／未設定時は非表示）＋ 組織名 |
| 右 | 「公開ページを見る」（S-10 へ）／「ログアウト」（S-12 へ） |

### 管理画面のサイドバー（ナビゲーション専用）

ダッシュボード（S-01）／イベント（S-02）／**資料（S-16）**／設定・データ管理（S-09）

- 各項目に**線画アイコン**を添える。アクティブ項目は淡い面（`--color-surface-sub`）のピルで示す
- 上部に**サービスロゴ（ACTIO）**、下部に**運用中の組織**（アバター＋組織名）を置く
- **設定・データ管理を開いているときは、5.4 のタブを子項目として展開表示する**（一般設定／カテゴリ管理／フォーム既定値／メールテンプレート／送信履歴／パスコード変更／データ管理）
- サービスロゴは `--color-mark-*` を使った SVG。外部画像は参照しない（制約7）

> ナビゲーション項目と、性質の異なる操作（外部遷移・セッション操作）を同一領域に並べない。
> 前者はサイドバー、後者はヘッダーに配置する。

### 参加者側ヘッダー（全公開画面に固定表示）

| 位置 | 内容 |
|---|---|
| 左 | 組織ロゴ ＋ 組織名（クリックで S-10 へ） |
| 右 | イベントを探す（S-10）／マイチケット（S-11）／主催者ログイン（S-12） |

現在のページに対応する項目をアクティブ表示する。

## 4. S-09 設定・データ管理のタブ構成

| タブ | 内容 | 対応機能 |
|---|---|---|
| 一般設定 | 組織名・ロゴ・ヘッダー画像・紹介文（**F-09**・余力）／**カテゴリ管理（追加・削除・並べ替え）**（**F-01 / F-26**・必須） | F-09 / F-01 / F-26 |
| フォーム既定値 | 既定の申込フォーム項目 | F-07 |
| メールテンプレート | 案内文の文面テンプレート | F-19 |
| **送信履歴** | `mailto:` 起動の記録の閲覧 | **F-19** |
| **パスコード変更** | 現パスコード確認 → 新パスコード設定 | **F-27** |
| データ管理 | JSONエクスポート／インポート／全初期化 | F-20, F-21 |

> **F-09（ロゴ設定）と F-19（送信履歴）は、機能一覧に存在するが設定・閲覧する画面がどこにも定義されていなかった。** S-09 に集約する。

## 5. 画面遷移

ルーティングは `location.hash` による。History API は `file://` で不安定なため使用しない。

```
【管理者：イベント作成〜公開】
S-12 ログイン → S-01 ダッシュボード → S-02 イベント一覧 → S-03 作成
  → S-05 プレビュー → S-03 公開 → S-04 イベント詳細

【参加者：正常系】
S-10 公開イベント一覧 → S-05 公開イベントページ → S-06 申込フォーム
  → S-13 申込内容確認 → S-07 申込完了 →（後日）S-11 申込照会

【参加者：入力内容の修正】
S-13 申込内容確認 → S-06 申込フォーム → S-13 申込内容確認

【参加者：申込の中断】
S-06 申込フォーム → S-05 公開イベントページ

【満席時】
定員に達した券種は S-05・S-06 で「満席」と表示し、選択不可とする。
S-06 から先へ進めない。

【参加者：後日のキャンセル】
S-05 または S-10 →「申込を確認する」→ S-11 申込照会 → キャンセル実行

【管理者：当日受付〜終了後】
S-04 → S-08 参加者一覧 →（受付コードで検索）→ チェックイン → CSV出力
S-02 → 複製 → S-03（次回イベント）

【異常系】
存在しないイベントID・不正なハッシュルート → S-14 エラー → S-10 公開イベント一覧
未認証で要認証画面へアクセス → S-12 ログイン
```

## 6. 余力機能の未実装時の既定挙動

余力枠は実装枠を確保していないため、**未実装の状態が既定**である。実装と記述が食い違わないよう、未実装時の挙動を定義する。
**F-09 と F-34 は v1.16 で実装済みだが、縮退時の拠り所として本表は残す。**

| 機能 | 未実装時の既定挙動 |
|---|---|
| **F-09 ロゴ** | 表示しない。組織名のテキストのみを表示する |
| **F-09 ヘッダー画像** | S-10 公開イベント一覧のヘッダーを**単色（CSS変数の既定色）で描画**する |
| **F-09 紹介文** | 表示しない。組織名のみを表示する |
| **F-34 運用資料の管理** | サイドバーの「資料」を表示しない（S-16 へ遷移できない） |

**縮退計画の順0を実行した場合、本表の既定挙動が納品時の仕様となる。** 試験書もこの状態を前提に作成する。
