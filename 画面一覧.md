# イベント主催・管理サービス 画面一覧

**サービス名**: ACTIO（アクティオ）
**版**: v1.19（2026/07/28）
**位置づけ**: 課題の成果物として独立して提出する画面一覧。**本ファイルが唯一の正**であり、`仕様書.md` には複製しない。

**関連文書**: `仕様書.md`（スコープ・データモデル・入力仕様・制約・デザイントークン・非機能要件）／`仕様決定シート.md`（入力仕様と業務ルールの確定値 D-01〜D-45）／`検討記録.md`（判断の経緯）／`task_manegement.md`（進行管理）

---

## 1. 画面一覧

| 画面ID | 画面名 | 認証 | 役割 | 主な遷移先 |
|---|---|---|---|---|
| S-01 | ダッシュボード | **要** | 全体状況の把握。上から ①**数値タイル4枚**（公開中／申込／定員充足率／チェックイン率）②カテゴリ別の開催配分（円グラフ）③開催予定の一覧④**シリーズ横断の動員推移（縦棒グラフ・左が第1回）**（改善⑤） | S-02, S-03, S-04, **S-09, S-16**（サイドバーから全管理画面へ） |
| S-02 | イベント一覧 | **要** | 検索・複製・状態確認・**ページング** | S-03, S-04 |
| S-03 | イベント作成／編集 | **要** | **5ステップのウィザード**（1 基本＝イベント名・開催形式／2 会場・配信／3 詳細＝日時・カテゴリ・カバー画像・説明文／4 チケット／5 その他＝カスタム項目・運営体制・公開状態）。ステップは直接クリックでも移動でき、入力値は保持される。**運営体制は S-04 のパネルからも編集できる** | S-04 |
| S-04 | イベント詳細（管理） | **要** | 申込状況と参加者への導線・告知文の生成（F-29）・共有リンクの生成（F-10）・**このイベントの資料**（F-34）・**運営体制**（F-35。パネル内の「編集」から追加・削除する） | **S-05(プレビュー), S-08, S-03, S-16** |
| S-05 | 公開イベントページ | 不要 | 外部向け表示・申込導線。**S-04 からのプレビューは別ルート（`#/preview/:id`・要認証）**で開き、申込導線を出さずプレビューである旨を帯で示す | S-06, **S-10, S-11** |
| S-06 | 申込フォーム | 不要 | 券種選択＋参加者情報入力 | **S-13, S-05** |
| S-07 | 申込完了 | 不要 | **受付コードとQRコードの表示**。**用途3つ（当日受付／申込内容の確認／キャンセル）を明示**し、S-11 マイチケットへ導線を置く | S-05, S-11, **S-10** |
| S-08 | 参加者一覧／受付 | **要** | 上部に**数値タイル4枚**（申込／参加確定＝受付済み／未受付／キャンセル）。検索・**ページング**・チェックイン・CSV出力・メッセージ送信（宛先選択）・参加者情報の編集・キャンセル処理（主催者側・F-13） | S-04 |
| S-09 | 設定・データ管理 | **要** | **7つのタブで構成（4章参照）** | S-01（サイドバーから全管理画面へ） |
| S-10 | 公開イベント一覧 | 不要 | 参加者側の**唯一の入口**。構成は上から ①**ヒーロー**（ヘッダー画像を全幅・角丸なしで表示し、キャッチコピーと検索バーを重ねる）②**公開中のイベント**（検索バーの条件で絞った公開中のものを全件、横並びカードで表示）③**カテゴリから探す**（②とは独立。カテゴリを選ぶと、**終了したイベントも含めて**その下に一覧する） | S-05, **S-11** |
| S-11 | **マイチケット** | 不要 | **メールアドレス＋パスワードでログイン**し、**同一メールアドレスの申込を全件表示**。各申込の内容確認とキャンセルが可能。**受付コードとQRコードを併記して表示する（QRは `state='confirmed'` かつ開催前の申込のみ。受付済みかどうかは問わない）**。終了済みイベントの申込も履歴として表示する（キャンセル操作は不可） | S-05, S-10 |
| S-12 | 主催者ログイン | — | 管理画面へのアクセス制御（パスコード入力）・初回起動時のパスコード初期設定・**未認証で要認証の画面にアクセスした際のリダイレクト先** | S-01 |
| S-13 | 申込内容確認 | 不要 | 申込前の入力内容確認 | S-07, **S-06** |
| S-14 | エラー | 不要 | 存在しないイベントID・不正なハッシュルートを指定された場合の表示 | S-10 |
| **S-16** | **資料** | **要** | **運用資料の登録・一覧・ダウンロード・削除。登録時に紐付け先イベントを選び（「組織共通」も可）、イベントで絞り込める。紐付けた資料は S-04 からも開ける（F-34）** | S-01, S-04 |

> **S-11 でメールアドレスのみでの一覧表示は行わない。** 他人のメールアドレスを入力すれば申込内容を閲覧できてしまうため、**必ず2要素で照合する。**
> **v1.17 で2要素目を受付コードからパスワード（D-43）へ変更した。** 受付コードは申込ごとに異なるため、
> 「同一メールの申込を全件見る」という要件と両立しなかったのが理由である。
> パスワードは申込フォームで設定し、同じメールアドレスの2回目以降は登録済みのものと照合する。
> **F-27 と同様、端末内での平文照合であり実セキュリティは持たない**（`仕様書.md` 10.2 参照）。
> 照会失敗時は「メールアドレスまたはパスワードが正しくありません」とし、**登録の有無を区別しない。**

> S-13 は追番のため一覧上は末尾に位置するが、**遷移順は S-06 と S-07 の間**である。既存IDの追跡性を保つため再採番はしない。

> **S-15（管理側マイチケット）は削除した。** 全券種の集計表であり S-01 ダッシュボードと内容が重複していたため、券種の状況は S-01 と S-04 で確認する（検討記録 5.1 #1）。

## 2. 認証要否（ルーティングガードの実装仕様）

| 認証 | 画面 |
|---|---|
| **要** | S-01, S-02, S-03, S-04, S-08, S-09, **S-16**、および **S-05 のプレビュー経路（`#/preview/:id`）** |
| 不要 | S-05（`#/public/:id`）, S-06, S-07, S-10, S-11, S-13, S-14 |
| — | S-12（ログイン画面自身） |

**未認証で「要」の画面にアクセスした場合は S-12 へリダイレクトする。** 結合試験の項目に直結する。

> **S-05 は経路によって認証要否が異なる。** 参加者が開く `#/public/:id` は認証不要、
> 主催者が S-04 から開く `#/preview/:id` は**要認証**で、申込導線を出さない。
> 同じ画面IDだがルーティングガードの扱いが違うため、試験項目も分けて用意する。

## 3. 共通レイアウト

### 管理画面のヘッダー（全管理画面に固定表示）

| 位置 | 内容 |
|---|---|
| 左 | 運用中の**組織名**（サービスロゴと組織ロゴはサイドバーに置くため、ここには出さない） |
| 右 | 「公開ページを見る」（S-10 へ）／「ログアウト」（S-12 へ） |

### 管理画面のフッター（全管理画面）

組織名とサービス名、および**データの保持場所の注記**（この端末のブラウザ内に保持している旨）を表示する。

### 管理画面のサイドバー（ナビゲーション専用）

ダッシュボード（S-01）／イベント（S-02）／**資料（S-16）**／設定・データ管理（S-09）

- 各項目に**線画アイコン**を添える。アクティブ項目は淡い面（`--color-surface-sub`）のピルで示す
- 上部に**サービスロゴ（ACTIO）**、下部に**運用中の組織**（アバター＋組織名）を置く
- **設定・データ管理の右横の ▾ で子項目（4章のタブ）を開閉する。** 開いていれば**どの画面にいても子項目が出て直接移動できる**（一般設定／カテゴリ管理／フォーム既定値／メールテンプレート／送信履歴／パスコード変更／データ管理）。選択中の子項目は、設定画面を開いているときだけ本文色＋中太で示す
- サービスロゴは `--color-mark-*` を使った SVG。外部画像は参照しない（制約7）

> ナビゲーション項目と、性質の異なる操作（外部遷移・セッション操作）を同一領域に並べない。
> 前者はサイドバー、後者はヘッダーに配置する。

### 参加者側ヘッダー（全公開画面に固定表示）

| 位置 | 内容 |
|---|---|
| 左 | **サービスロゴ（ACTIO）** ＋ 区切り ＋ 組織ロゴ ＋ 組織名（クリックで S-10 へ） |
| 右 | イベントを探す（S-10）／マイチケット（S-11）／主催者ログイン（S-12） |

現在のページに対応する項目をアクティブ表示する。

### 申込の状態表示

**受付（チェックイン）が済んで初めて「参加確定」とする。** それまでは「申し込み済み」。

| 状態 | 表示 | セマンティックカラー |
|---|---|---|
| `confirmed` かつ `checkedInAt = null` | 申し込み済み | `warning` |
| `confirmed` かつ `checkedInAt` あり | 参加確定 | `success` |
| `cancelled` | キャンセル済み | `danger` |

S-08 参加者一覧と S-11 マイチケットで同じ基準を使う。

### 一覧テーブルの列幅

**行の内容で列幅が動くとボタンの位置がずれる**ため、一覧は `table-layout: fixed` と列幅指定で固定する。
参加者一覧では**キャンセル済みの行でも操作ボタンを消さず無効化する**（ボタンの数が変わると位置がずれるため）。
**件数の表示は表ではなく数値タイルを使う。** 表だと値の文字数で列幅が変わり、受付・受付取消のたびに
上部の集計や下のツールバーが動いてしまう（Issue #6）。タイルは幅が決まっているため位置が固定される。
イベント一覧の「シリーズ」は**独立した列**とする（イベント名の直後に置くと名前の長さで位置がずれるため）。
**最終行にも罫線を引く**（一番下だけ線が消えていると途中で止まって見えるため）。
**管理画面と同じ高さ・同じロゴ位置・同じ字送り**とし、サイドバーの有無以外は見た目を揃える。

> **「お気に入り」「通知」は設けない。** 参加者アカウントを前提とする機能であり、
> `仕様書.md` 8章 #10 で実現不可能としている領域に当たる（検討記録 7.3）。

## 4. S-09 設定・データ管理のタブ構成

**7タブ。** サイドバーからも直接開ける。

| # | タブ | 内容 | 対応機能 |
|---|---|---|---|
| 1 | 一般設定 | 組織名・組織ロゴ・ヘッダー画像（**紹介文は廃止**） | F-09 |
| 2 | **カテゴリ管理** | 追加・削除・並べ替え。**上限20件、使用中のカテゴリは削除不可** | **F-01 / F-26**（D-12） |
| 3 | フォーム既定値 | 新規イベントに引き継ぐカスタム項目の追加・削除・並べ替え | F-07（D-42） |
| 4 | メールテンプレート | 案内・リマインド・告知文の文面。**差し込み文字列をクリックで挿入できる** | F-19 |
| 5 | 送信履歴 | `mailto:` 起動の記録の閲覧 | F-19 |
| 6 | パスコード変更 | 現パスコード確認 → 新パスコード設定 | F-27（D-33） |
| 7 | データ管理 | JSON書き出し／読み込み（**資料ファイルを含めるかを選択・既定オフ**）／デモデータ投入／全データ初期化 | F-20, F-21 |

> **F-09（組織ブランディング）と F-19（送信履歴）は、機能一覧に存在するが設定・閲覧する画面がどこにも定義されていなかった。** S-09 に集約する。
> **カテゴリ管理は D-12 でカテゴリを主催者定義に変えたことで必要になった**ため、独立したタブとする。

## 5. 画面遷移

ルーティングは `location.hash` による。History API は `file://` で不安定なため使用しない。

```
【管理者：イベント作成〜公開】
S-12 ログイン → S-01 ダッシュボード → S-02 イベント一覧 → S-03 作成
  → 5ステップを順に入力 → S-04 イベント詳細
  ※ 途中で「下書きとして保存」すると S-04 へ抜ける
  ※ S-04 の「プレビュー」は #/preview/:id（要認証・申込導線なし）で S-05 を開く

【管理者：資料の管理】
S-01 → サイドバー「資料」→ S-16（登録・ダウンロード・削除）

【参加者：正常系】
S-10 公開イベント一覧 → S-05 公開イベントページ → S-06 申込フォーム
  → S-13 申込内容確認 → S-07 申込完了 →（後日）S-11 マイチケット

【参加者：入力内容の修正】
S-13 申込内容確認 → S-06 申込フォーム → S-13 申込内容確認

【参加者：申込の中断】
S-06 申込フォーム → S-05 公開イベントページ

【満席時】
定員に達した券種は S-05 公開イベントページで「満席」と表示する。
S-06 申込フォームでは**選択肢そのものに出さない**（申し込める券種が1つも無ければ
「現在申し込める券種がありません」を表示し、先へ進めない）。

【参加者：後日のキャンセル】
ヘッダーの「マイチケット」→ S-11（メールアドレス＋パスワードでログイン）
  → 同一メールの申込を全件表示 → キャンセル実行
  ※ 終了済みイベントと受付済みの申込は履歴表示のみでキャンセル不可

【管理者：当日受付〜終了後】
S-04 → S-08 参加者一覧 →（受付コードで検索）→ チェックイン → CSV出力
S-02 → 複製 → S-03（次回イベント）

【異常系】
存在しないイベントID・不正なハッシュルート → S-14 エラー → S-10 公開イベント一覧
未認証で要認証画面へアクセス → S-12 ログイン
```

## 6. 余力機能の未実装時の既定挙動

**F-09（v1.17）と F-34（v1.16）はいずれも実装済みで、現時点で余力枠は残っていない。**
縮退が必要になった場合の拠り所として本表を残す。

| 機能 | 未実装時の既定挙動 |
|---|---|
| **F-09 ロゴ** | 表示しない。組織名のテキストのみを表示する |
| **F-09 ヘッダー画像** | S-10 のヒーローを**濃色のグラデーションで描画**する（`.phero--plain`） |
| **F-34 運用資料の管理** | サイドバーの「資料」を表示しない（S-16 へ遷移できない） |

**縮退を実行した場合、本表の既定挙動が納品時の仕様となる。** ただし現時点では実装済みのため、試験書は実装ありの状態を前提に作成する。
