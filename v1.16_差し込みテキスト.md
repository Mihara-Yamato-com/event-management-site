# v1.16 差し込み用テキスト

各ブロックを、指定の場所へそのまま貼り付ける。**本ファイルは作業用のため、貼り付け後に破棄してよい。**

---
---

# ■ 差し込み1 — `検討記録.md` に新章として追加

## 5. 実機レビュー記録（Day2 / 2026-07-28）

Day1 に作成した仕様に基づき実装した `index.html` を実際に操作し、
仕様の妥当性を検証した。以下はその結果と、それに伴う仕様変更の判断である。

### 5.1 実機で判明した問題

| # | 観察された事象 | 原因 | 対応 |
|---|---|---|---|
| 1 | 管理側の「マイチケット」タブが全券種の集計表になっており、S-01 ダッシュボードと内容が重複していた | 参加者向けの概念（マイチケット）を管理側の画面として設計してしまっていた | **画面ごと削除**し、券種の状況は S-01 と S-04 で確認する |
| 2 | 参加者側にナビゲーションがなく、イベント一覧に戻る導線とマイチケットへの導線がなかった | 参加者側のシェルをヘッダー1本で済ませていた | ヘッダーに**3導線**（イベントを探す／マイチケット／主催者ログイン）を追加 |
| 3 | 管理側サイドバーで「公開ページを見る」「ログアウト」の表示が崩れていた | ナビゲーション項目と、性質の異なる操作（外部遷移・セッション操作）を同一のスタイルで並べていた | **ヘッダーを新設**し、性質で配置を分離。サイドバーはナビ専用とする |
| 4 | 申込完了画面の受付コードについて、何に使うのかが伝わらなかった | 「控えてください」としか書いていなかった | 用途（当日受付・申込確認・キャンセル）を明示し、マイチケットへの導線を設置 |
| 5 | 既定の申込フォーム項目に「所属部署」「年数」があった | 社内イベントを前提に設計していた | 自社主催であっても社員限定とは限らないため**削除**。汎用的な3項目に差し替え |
| 6 | 申込のキャンセルが受付コード1件ずつの照会でしかできなかった | S-11 を単票の照会画面として設計していた | 照合後に**同一メールアドレスの申込を全件表示**する形に再構成 |

### 5.2 マルチテナント化の検討と見送り

実機を操作するなかで、「他社や個人も主催者として利用でき、参加者が主催者を横断して
イベントを探せる形にできないか」という要望が挙がった。検討の結果、**本課題の構成では
実現不可能**と判断した。

**理由**: データは各PCのブラウザ内の IndexedDB に格納されており、
主催者間・参加者間でデータを共有する手段が存在しない。
A社のPCで作成したイベントは、B社のPCからは存在を認識できない。
同一の HTML ファイルを配布しても、内部のデータは共有されない。

これに連なる以下も、同じ理由により実現できない。

- 参加者・主催者のアカウント登録とロール分離
- 主催者グループの作成
- 主催者を横断した公開イベント一覧

**この検討により、Peatix の構造に対する理解が更新された。**
Peatix がイベント作成前にグループ作成を必須としているのは、
複数の主催者が同一プラットフォーム上でイベントを開催する**マルチテナント構成だから**である。
グループは主催者を識別する名前空間として機能している。
本サービスは単一組織での運用を前提とするため、この名前空間が不要となり、
イベントを直接作成できる。**機能不足ではなく、前提の違いによる構造の単純化である。**

（カバレッジマップ E区分「主催者：グループ」の判断根拠として本節を参照する）

### 5.3 改善④（キャンセル待ちの自動繰り上げ）の取り下げ

余力枠として確保していたが、実機レビューで判明した導線の問題（5.1 の #2・#4・#6）を
優先すべきと判断し、**対象外に変更**した。

改善①〜⑧のうち④を落とすため、**Peatix に対する改善は7点**となる。
満席時の挙動は「券種を選択不可とし、満席である旨を表示する」で成立しており、
機能として破綻はしない。

### 5.4 レビューを踏まえた実装方針

| 項目 | 内容 | 工数 |
|---|---|---|
| S-15 削除・サイドバー整理 | 5.1 #1・#3 | 0.25h |
| 管理側ヘッダー新設 | 5.1 #3 | 1.0h |
| S-11 マイチケット再構成 | 5.1 #6 | 0.5h |
| 参加者側ヘッダー3導線 | 5.1 #2 | 0.5h |
| 受付コードの説明改善 | 5.1 #4 | 0.1h |
| 既定フォーム項目の差し替え | 5.1 #5 | 0.1h |
| F-34 資料管理の実装 | 余力枠から前倒し | 1.0h |
| S-10 サービス概要の追加 | — | 0.3h |

**合計 3.75h。** 改善④の実装枠（1.5h）を上記へ振り替えることで、Day3 の枠内に収める。

---
---

# ■ 差し込み2 — `仕様書.md` 4章 機能一覧

## F-14 の行を削除し、「検討のうえ対象外としたもの」へ以下を追加

| 対象 | 理由 |
|---|---|
| **キャンセル待ちと自動繰り上げ（旧 F-14・改善④）** | 余力枠として確保していたが、Day2 の実機レビューで判明した導線の問題を優先し、実装枠を振り替えたため対象外とした。**満席時は券種を選択不可とし「この券種は満席です」と表示する**（実装済みの既定挙動により機能として成立する） |
| **複数主催者による利用・アカウント登録・グループ作成** | データがローカルの IndexedDB に閉じており、主催者間でデータを共有できないため実現不可能。詳細は 8章 および 検討記録 5.2 を参照 |

## F-34 の優先度を「余力」から「必須」へ変更

| 機能ID | 機能名 | 優先度 |
|---|---|---|
| F-34 | 運用資料の管理（登録・一覧・ダウンロード・削除） | **必須** |

## 縮退計画 — 順0 の記述を変更

- 変更前: `**余力枠すべて**（F-09 組織ブランディング / F-14 キャンセル待ち / F-31 残席表示 / F-32 キャンセルポリシー / F-34 ファイル管理）`
- 変更後: `**余力枠すべて**（F-31 残席表示 / F-32 キャンセルポリシー）。F-09 と F-34 は実装済み、F-14 は対象外へ移動`

---
---

# ■ 差し込み3 — `仕様書.md` 5章 画面一覧

## S-15 の行を削除

## S-11 の役割を差し替え

| 画面ID | 画面名 | 認証 | 役割 | 主な遷移先 |
|---|---|---|---|---|
| S-11 | **マイチケット** | 不要 | 受付コード＋メールアドレスで照合し、**同一メールアドレスの申込を全件表示**。各申込の内容確認とキャンセルが可能。終了済みイベントの申込も履歴として表示する（キャンセル操作は不可） | S-05, S-10 |

> **メールアドレスのみでの一覧表示は行わない。** 他人のメールアドレスを入力すれば申込内容を閲覧できてしまうため、受付コードとの2要素を維持する。

## S-16 を追加

| 画面ID | 画面名 | 認証 | 役割 | 主な遷移先 |
|---|---|---|---|---|
| **S-16** | **資料** | 要 | 組織の運用資料の登録・一覧・ダウンロード・削除。**イベント単位ではなく組織全体**の資料置き場 | S-01 |

## 管理画面の共通レイアウトを追加

### ヘッダー（全管理画面に固定表示）

| 位置 | 内容 |
|---|---|
| 左 | 組織ロゴ（`settings.org.logo`／未設定時は非表示）＋ 組織名 |
| 右 | 「公開ページを見る」（S-10 へ）／「ログアウト」（S-12 へ） |

### サイドバー（ナビゲーション専用）

ダッシュボード（S-01）／イベント（S-02）／**資料（S-16）**／設定・データ管理（S-09）

> ナビゲーション項目と、性質の異なる操作（外部遷移・セッション操作）を同一領域に並べない。
> 前者はサイドバー、後者はヘッダーに配置する。

### 参加者側ヘッダー（全公開画面に固定表示）

| 位置 | 内容 |
|---|---|
| 左 | 組織ロゴ ＋ 組織名（クリックで S-10 へ） |
| 右 | イベントを探す（S-10）／マイチケット（S-11）／主催者ログイン（S-12） |

現在のページに対応する項目をアクティブ表示する。

## 「余力機能の未実装時の既定挙動」から F-14 の行を削除

対象外へ移動したため。

## 申込フローの【満席時】を1パターンに統一

```
【満席時】
定員に達した券種は S-05・S-06 で「満席」と表示し、選択不可とする。
S-06 から先へ進めない。
```

（【満席時・F-14 実装時】の記述は削除する）

---
---

# ■ 差し込み4 — `仕様書.md` 6章 データモデル

## `files` ストアを追加

**スキーマバージョンを +1 し、`onupgradeneeded` で既存データを保持したまま新ストアを作成する。**

| ストア | keyPath | インデックス |
|---|---|---|
| `files` | `id` | `createdAt` |

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | string | 主キー |
| `name` | string | ファイル名（100文字以内） |
| `note` | string | 説明（200文字以内） |
| `mime` | string | MIME タイプ |
| `size` | number | バイト数 |
| `blob` | Blob | ファイル本体。**リサイズは行わず原本を保持する** |
| `createdAt` | string | ISO 8601 |

> カバー画像・組織ロゴ・ヘッダー画像（D-15 / D-37 / D-38）と異なり、
> 運用資料は原本を保持する必要があるためリサイズしない。

## JSONエクスポート／インポート（F-20）の対象に `files` を追加

`files` ストアも base64 変換の対象とする。
**ただしエクスポートJSONが肥大化するため、エクスポート時に「資料ファイルを含める」
チェックボックスを設け、既定はオフとする。**（5MB × 20件で100MB超となり `JSON.stringify` が破綻するため）

---
---

# ■ 差し込み5 — `仕様書.md` 7章 入力仕様

## D-40 資料ファイル（F-34 / S-16）

| ID | 項目 | 必須 | 形式 | 制約・初期値 |
|---|---|---|---|---|
| D-40 | 資料ファイル | ● | file | `pdf, png, jpeg, xlsx, docx`／**1ファイル5MB以下**／**登録上限20件** |
| D-41 | 資料の説明 | | text | 200文字 |

## D-42 既定の申込フォーム項目（差し替え）

「所属部署」「年数」を削除し、以下を既定とする。

| 項目名 | 型 | 選択肢 |
|---|---|---|
| 参加のきっかけ | select | 社内の案内／知人の紹介／SNS／その他 |
| 懇親会に参加する | checkbox | — |
| 質問・要望 | text | — |

> 自社主催であっても社員限定のイベントとは限らないため、所属部署・年数を既定項目としない。

## エラーメッセージに追加

| 種別 | 文面 |
|---|---|
| 資料のサイズ超過 | 「ファイルは5MB以内のものを選択してください」 |
| 資料の形式不正 | 「対応していないファイル形式です」 |
| 資料の件数超過 | 「資料は20件以内で登録してください」 |

---
---

# ■ 差し込み6 — `仕様書.md` 8章 制約と代替案

## 実現不可能領域に追加

| 領域 | 判断 |
|---|---|
| **複数の主催者（他社・個人）による利用** | **実現不可能。** データが各PCの IndexedDB に閉じており、主催者間・参加者間でデータを共有できない。共有データベースと API サーバが必須 |
| **参加者・主催者のアカウント登録とロール分離** | 同上。認証基盤が必要 |
| **主催者グループの作成** | マルチテナント構成を前提とする機能。単一組織での運用では名前空間が不要となり、階層自体が不要（検討記録 5.2） |
| **主催者を横断した公開イベント一覧** | 他主催者のデータを参照できないため成立しない |
| **QRコードの発行** | 外部ライブラリを使用できないため、誤り訂正符号を含む生成処理の自前実装が必要となり、期間内の工数に見合わない。**英数字8桁の受付コードで代替する** |

## 本番構成案に追加

API サーバ ＋ RDB（主催者・イベント・申込を別テーブルで管理）＋ 認証基盤（主催者／参加者のロール分離）。
主催者テーブルを設けることでマルチテナント構成が成立し、グループ機能・横断検索・アカウント登録が
いずれも実現可能となる。**本課題の成果物は、この構成における画面と業務ロジックの検証に相当する。**

---
---

# ■ 差し込み7 — `task_manegement.md` 改訂履歴

| 版 | 日時 | 変更内容 | 理由 |
|---|---|---|---|
| v1.16 | 7/28 | **S-15 マイチケット（管理側）を削除。** 管理側にヘッダーを新設しサイドバーをナビ専用化。**S-11 を参加者向けマイチケットとして再構成**（同一メールの申込を全件表示・キャンセル可）。参加者側ヘッダーに3導線を追加。**F-34 資料管理を必須へ格上げし S-16 を新設**（`files` ストア追加）。S-10 にサービス概要を追加。既定の申込フォーム項目を差し替え。**F-14 キャンセル待ちを対象外へ変更（改善④取り下げ・改善は7点に）。** マルチテナント化の検討結果を仕様書8章と検討記録5章へ記録 | **Day2 に実機レビューを実施し、6件の問題を確認**（詳細は検討記録 5.1）。管理側マイチケットが S-01 と重複し専用タブに値しないこと、参加者側の導線が不足していることが判明。あわせてマルチテナント化の要望が挙がったが、データがローカルに閉じる構成では原理的に実現不可能なため、判断の記録として残した |

---
---

# ■ 貼り付け後に Claude Code へ渡す指示

````
仕様書.md を v1.16 に更新しました。以下を実施してください。

1. 仕様書 4章・5章・6章・7章の v1.16 変更点を index.html に反映する
   （変更点の一覧は task_manegement.md の改訂履歴 v1.16 を参照）
2. 検討記録 5.4 の実装方針に記載された8項目を実装する

**6章の `files` ストア追加ではスキーマバージョンを +1 し、
`onupgradeneeded` で既存データを保持したまま新ストアを作成すること。**
既存のイベント・申込データを失わないことを、デモデータで確認してから実データで検証する。
````
