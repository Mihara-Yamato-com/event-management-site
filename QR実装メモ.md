# QRコード自前実装メモ

**作成日**: 2026/07/28
**位置づけ**: 実装作業用の技術資料。**実装完了後も残す**（外部ライブラリを用いずQRを生成した根拠と、再検証用の期待値を保持するため）
**対象機能**: F-24（受付コード発行・電子チケット表示）の範囲内。**新しい機能IDと画面は作らない**

---

## 1. 実装範囲

| ステップ | 対応 | 理由 |
|---|---|---|
| ① 受付コードを生成 | **実装済み** | `generateCode()`（D-32・英大文字＋数字8桁） |
| ② IndexedDBへ保存・未受付で初期化 | **実装済み** | `entries.code` / `checkedInAt: null` |
| **③ QRコードを生成・表示** | **← 本作業** | — |
| ④ カメラ起動 | **対象外** | `file://` はセキュアコンテキストではないため `getUserMedia` が使用できない |
| ⑤ QRを読み取る | **対象外** | デコーダは画像の二値化・射影補正・パターン検出を要し、外部ライブラリなしでの実装は工数に見合わない |
| ⑥ 受付コードで照合 | **実装済み** | S-08 の検索 |
| ⑦ チェックイン処理 | **実装済み** | S-08 の受付ボタン（`toggleCheckIn`） |
| ⑧ 結果を表示 | **実装済み** | トースト表示 |

**⑥⑦⑧はすでに動作しており、④⑤の欠落を受付コードの手入力で代替している。**

### QRの中身

**既存の8桁受付コードをそのまま格納する。** `ACTIO-20260804-A1B2C3` のような形式には変更しない。
D-32 で確定済みであり既存の申込データもこの形式のため、変更すると決定シート・仕様書・既存データすべてに波及する。

### 本番構成での位置づけ

読み取り側（HTTPS配信 ＋ デコーダ ＋ 照合API）が揃えば、**参加者側の変更なしにカメラ受付が成立する。**
発行するコードも現状のまま使える。本実装は本番構成への接続点として機能する。

---

## 2. 技術仕様 — バージョン1・誤り訂正レベルL・英数字モード固定

8桁の英数字は英数字モードでバージョン1（21×21）に収まる（上限25文字）。
**バージョン1限定とすることで、位置合わせパターン・バージョン情報・ブロック分割が不要になる。**
汎用のQR生成器は作らない。

### 2.1 英数字モードのエンコード

| 項目 | 値 |
|---|---|
| モード指示子 | `0010`（4ビット） |
| 文字数指示子 | 9ビット（バージョン1〜9の英数字モード） |
| 文字値 | `0-9`→0〜9、`A-Z`→10〜35、`空白`→36、`$`→37、`%`→38、`*`→39、`+`→40、`-`→41、`.`→42、`/`→43、`:`→44 |
| 2文字ペア | `前の値 × 45 + 後の値` を **11ビット** |
| 端数1文字 | **6ビット** |
| 終端 | `0000` |
| バイト境界 | 0で埋める |
| 埋め草 | `0xEC` `0x11` を交互に、**データ19語**になるまで |

### 2.2 誤り訂正（Reed-Solomon）

| 項目 | 値 |
|---|---|
| GF(256) 原始多項式 | **`0x11D`** |
| データ語数 | **19** |
| 誤り訂正語数 | **7** |
| ブロック | **1**（分割・インターリーブなし） |
| 総語数 | 26 |

**生成多項式の係数（次数7）— 検証済みの固定値**

```
[1, 127, 122, 154, 164, 11, 68, 117]
```

> **この配列を定数として持ち、生成多項式の計算処理は実装しない。**
> GF(256)上の多項式乗算で導出できるが、誤りが混入しても見た目は正常なQRになるため、検証済みの値を使う。

### 2.3 形式情報 — 検証済みの固定値

レベルLの8マスク分をテーブルとして持つ。**BCH符号化処理は実装しない。**

| マスク | 15ビット |
|---|---|
| 0 | `111011111000100` |
| 1 | `111001011110011` |
| 2 | `111110110101010` |
| 3 | `111100010011101` |
| 4 | `110011000101111` |
| 5 | `110001100011000` |
| 6 | `110110001000001` |
| 7 | `110100101110110` |

### 2.4 機能パターンの配置（21×21・`row, col` は0起点）

| 要素 | 配置 |
|---|---|
| 位置検出パターン（7×7） | 左上 `(0,0)` / 右上 `(0,14)` / 左下 `(14,0)` |
| 分離パターン | 各位置検出パターンの外周1モジュールを白 |
| タイミングパターン | **6行目**と**6列目**に黒白交互（位置検出パターンの間） |
| 固定の黒モジュール | **`(13, 8)`** |
| 形式情報 | 位置検出パターン周辺の所定位置 |
| 位置合わせパターン | **なし**（バージョン2以降のみ） |
| バージョン情報 | **なし**（バージョン7以降のみ） |

### 2.5 データ配置

右下から開始し、**2モジュール幅の列**を右から左へ。列ごとに上向き・下向きを交互に切り替える。
**6列目（タイミングパターン）はスキップする。** 機能パターンの位置には書き込まない。

### 2.6 マスク処理

8種すべてを適用し、**ペナルティ評価が最小のものを採用する。**
`(i, j)` は `(row, col)`。条件が真のモジュールを反転する。

| # | 条件 |
|---|---|
| 0 | `(i + j) % 2 === 0` |
| 1 | `i % 2 === 0` |
| 2 | `j % 3 === 0` |
| 3 | `(i + j) % 3 === 0` |
| 4 | `(Math.floor(i / 2) + Math.floor(j / 3)) % 2 === 0` |
| 5 | `((i * j) % 2) + ((i * j) % 3) === 0` |
| 6 | `(((i * j) % 2) + ((i * j) % 3)) % 2 === 0` |
| 7 | `(((i + j) % 2) + ((i * j) % 3)) % 2 === 0` |

**ペナルティ規則**

| 規則 | 内容 | 加点 |
|---|---|---|
| 1 | 行または列で同色が5個以上連続 | `3 + (連続数 - 5)` |
| 2 | 同色の2×2ブロック | 1ブロックあたり `3` |
| 3 | 行または列に `10111010000` または `00001011101` のパターン | 1件あたり `40` |
| 4 | 黒モジュールの比率の50%からの偏り | `Math.floor(Math.abs(比率 - 50) / 5) * 10` |

**形式情報は評価対象に含める**（マスク適用後に書き込み、そのうえで評価する）。

### 2.7 描画

**SVGで出力する。canvasは使用しない**（制約8を回避し、拡大時も鮮明に保つため）。

- `viewBox="0 0 29 29"`（21モジュール ＋ 静押域4モジュール×2）
- 黒モジュールを `<rect width="1" height="1">` として配置
- 白モジュールは描かず、背景の `<rect>` で表現
- 色は `--color-text` と `--color-bg` を使う。**`#000` `#fff` を直値で書かない**（仕様書9章）
- `shape-rendering="crispEdges"` を付ける

---

## 3. 単体テストの期待値（検証済み）

**実装後、最初にこれを通す。** 一致しないうちは配置・マスクの工程に進まない。

### 入力 `H7K2M9XQ`

```
データ19語(hex):   20 43 04 70 CF 9E F3 80 EC 11 EC 11 EC 11 EC 11 EC 11 EC
誤り訂正7語(hex):  97 D9 AA 35 92 32 55
```

### 入力 `ABCDEFGH`

```
データ19語(hex):   20 41 CD 45 2A 15 70 80 EC 11 EC 11 EC 11 EC 11 EC 11 EC
誤り訂正7語(hex):  86 CC 87 8E 4D 0D 34
```

> **この2件が通れば、エンコードと誤り訂正は正しい。** 残るリスクはマスク選択と配置に絞られ、
> それはスマートフォンで読み取れるかどうかで判定できる。

---

## 4. 作業手順

### フェーズ1 — 単体検証（`qr-test.html` で実装。index.html は触らない）

1. 2章の仕様で生成処理を実装する
2. 3章のアサーションを埋め込み、画面に PASS / FAIL を表示する
3. **ダブルクリックで開き**、以下すべてを満たすことを確認する

| # | 完了条件 |
|---|---|
| 1 | 単体テスト2件が **PASS** |
| 2 | **表示されたQRをスマートフォンのカメラで読み取り、`H7K2M9XQ` が得られる** |
| 3 | 別の8桁コードを3件以上入力し、いずれも読み取れる |
| 4 | 拡大・縮小してもモジュールの境界がぼやけない |

> **条件2が最重要。** 誤り訂正やマスク選択が1ビットずれていても見た目は正常なQRになる。
> **読み取れないQRは、無いより悪い。** 読めるまでフェーズ2に進まない。

### フェーズ2 — index.html への組み込み

1. 生成処理を `index.html` の `<script>` 内へ移す。**アサーションは移さない**

```js
/* F-24 受付コードのQRコード生成（バージョン1・レベルL・英数字モード固定）
   読み取りは対象外（仕様書8章）。表示のみ */
function qrSvg(text) { ... }
```

2. **S-11 マイチケット** — 各申込カードの受付コードの隣に配置

| 条件 | 挙動 |
|---|---|
| サイズ | 160×160 程度 |
| 受付コードの文字表示 | **残す**（読み取れない環境の代替手段として必須） |
| QRの下 | 「当日の受付でこのコードを提示してください」 |
| `state` が `cancelled` | **QRを表示しない** |
| 終了済みイベント | **QRを表示しない**（履歴表示のみ） |

3. **S-07 申込完了** — 受付コードとQRの両方を表示。S-11 と同じ描画処理を使う

4. **`qr-test.html` を削除する**（成果物は index.html のみ・制約2）

---

## 5. 試験項目（3件・決定シートへ追記する）

| # | 手順 | 期待結果 |
|---|---|---|
| 1 | マイチケット（S-11）にログインし、表示されたQRをスマートフォンのカメラで読み取る | **画面に表示されている受付コードと同一の8桁の英数字が、スマートフォン上に表示される** |
| 2 | キャンセル済みの申込をマイチケットで表示する | QRコードが表示されない |
| 3 | 終了済みイベントの申込をマイチケットで表示する | QRコードが表示されない |

> 項目1は**別デバイスでの確認**となる唯一の試験項目。エビデンスの残し方（スマートフォンの画面写真を貼るか、確認済みのチェックのみか）を試験書作成時に決めておく。備考欄に「スマートフォンでの確認」と明記する。

**スマートフォンのカメラは受付コードのテキストを表示するだけであり、本システムは何も出力しない。**
受付処理（⑦）は S-08 の受付ボタンで行う。

---

## 6. 各文書への反映内容

**以下を各文書へ転記する。転記後、本章は不要になるが削除しなくてよい。**

### 6.1 仕様書 8章 — 実現不可能領域

**「QRコードの発行」の行を削除**し、以下に差し替える。

| 領域 | 判断 |
|---|---|
| **QRコードの読み取りによる受付** | **実現不可能。** ① `file://` はセキュアコンテキストではないため `getUserMedia` によるカメラ起動ができない ② デコーダは画像の二値化・射影補正・パターン検出を要し、外部ライブラリなしでの実装は工数に見合わない。**受付は S-08 での受付コード検索で行う** |

### 6.2 仕様書 8.1 — 追記

> **QRコードの表示については当初「外部ライブラリを使用できないため対象外」と判断していたが、Day2 に再検討した。**
> 受付コードが8桁の英数字であるため**バージョン1・レベルL・英数字モード**で足り、
> 位置合わせパターン・バージョン情報・ブロック分割が不要になることが判明したため、自前実装した（技術詳細は `QR実装メモ.md`）。
> **読み取りは対象外のまま**であり、受付は受付コードの手入力による照合となる。
>
> **カメラによる受付が不可能な理由は、マルチテナント化を見送った理由と同一の構造にある。**
> どちらも「データと実行環境が端末に閉じている」ことに帰着する（検討記録 5.2）。

### 6.3 仕様書 8.2 — 本番構成案に追記

> カメラ受付を実現する場合、**HTTPS配信 ＋ QRデコーダ ＋ 照合API** が必要になる。
> **照合と状態更新はサーバ側で1つの処理として実行する。** クライアント側で判定すると、
> 複数の受付端末が同時に動作した際に二重受付が発生する。

### 6.4 機能一覧 — F-24 の機能名

- 変更前: `受付コード発行・電子チケット表示。**用途は当日受付・申込内容の確認・キャンセルの3つ**`
- 変更後: `受付コード発行・電子チケット表示（**受付コードとQRコードを併記**）。**用途は当日受付・申込内容の確認・キャンセルの3つ**。QRの読み取りは対象外`

### 6.5 機能一覧 §6 — 検討のうえ対象外としたもの

| 対象 | 理由 |
|---|---|
| **QRコードの読み取りによる受付** | `file://` ではカメラを起動できず、デコーダの自前実装も工数に見合わないため対象外。**QRの表示は自前実装で対応済み**（仕様書 8.1）。受付は S-08 での受付コード検索で行う |

### 6.6 画面一覧

- **S-11** の役割の `各申込の内容確認とキャンセルが可能` の後に追記
  → `**受付コードとQRコードを併記して表示する（QRは参加確定かつ開催前の申込のみ）**`
- **S-07** の役割の `受付コードの表示` を `**受付コードとQRコードの表示**` に変更

### 6.7 検討記録 2章 — カバレッジマップ

- 「QRコード付きチケットの表示」の判定を **△ → ○** に変更
- 実現欄を `バージョン1・レベルL・英数字モードで自前実装。読み取りは対象外` とする
- **カバレッジ集計を再計算する**

### 6.8 仕様決定シート — 試験項目

5章の3件を追記する。**試験項目は合計26項目**となる（23＋3）。

### 6.9 改訂履歴

| 版 | 日時 | 変更内容 | 理由 |
|---|---|---|---|
| v1.20 | 7/28 | **受付コードのQRコード表示を実装**（バージョン1・レベルL・英数字モード／SVG描画）。S-11 マイチケットと S-07 申込完了に併記。仕様書8章から「QRコードの発行」を削除し、**読み取りのみ対象外**として残した。カバレッジマップ #29 を △→○ に変更。技術資料として `QR実装メモ.md` を追加 | 当初は外部ライブラリが使えないため対象外としていたが、**受付コードが8桁の英数字であるためバージョン1で足りることが判明**し、自前実装が可能と判断した。あわせて読み取りが不可能な理由を「工数」から「`file://` がセキュアコンテキストでないこと」に改めた（より本質的な制約であるため）。機能IDと画面は増やさず F-24 の範囲内で対応 |
