# イベント主催・管理サービス 検討記録

Peatix との比較検討、確認事項、自社運用要件のヒアリング内容をまとめる。
**「作れるか」「作るべきか」を判断した過程の記録**であり、確定した仕様は `仕様書.md` に置く。

**関連文書**: `task_manegement.md`（WBS・進行方針）／`仕様書.md`（確定仕様）／`CLAUDE.md`（実装規約）

---

# 1. Peatix の課題と本サービスでの改善（改善マトリクス）

出典はPeatix公式ヘルプおよび公開情報。◎＝本課題で実装、△＝一部実装、×＝スコープ外。

**実装確度**: ◎確定＝必須機能に内包済みで実装が確約されている／○予定＝Day4 T4-1 の枠で実装する／△候補＝Day3までの進捗次第／×＝対象外

| ID | Peatixの現状 | 業務上の不便 | 本サービスでの改善 | 実装確度 | 優先度 |
|---|---|---|---|---|---|
| ① | 参加者のアカウント登録メールアドレスは主催者に開示されない。連絡先が必要な場合はフォーム機能を別途設定する必要がある | フォーム設定を忘れると参加者に連絡が取れない。設定漏れが事故に直結する | 氏名・メールアドレスを申込フォームの標準必須項目として固定。設定漏れが構造的に起きない | **◎ 確定**（F-06に内包） | 高 |
| ② | フォームの項目設計が主催者任せで、券種ごとの出し分けなど細かい制御がしづらい | 部署・参加動機・食事制限など、イベントごとに必要な項目の作り込みに手間がかかる | 任意項目を追加・並べ替え・必須設定できるフォームビルダーを提供。**※券種ごとの項目出し分けは本サービスでも対象外** | **◎ 確定**（F-07に内包） | 高 |
| ③ | イベントページの細かなレイアウト設定やデザイン調整には対応していない | 自社ブランディングができず、Peatixのテンプレートに見える | **自社ロゴの登録のみに縮小**（テーマカラー設定は廃止）。配色はデザイントークンの初期値で固定 | △ 候補（**余力枠**） | 中 |
| ~~④~~ | 満席後のキャンセル発生時、繰り上げは主催者の手作業 | キャンセルのたびに手動で連絡・確定処理が必要 | ~~キャンセル検知時に待機列の先頭を自動で参加確定に繰り上げ~~ | **× 取り下げ**（F-14 廃止） | — |
| ⑤ | 複数回開催イベントは受付時に注意点があり、シリーズ横断の実績把握がしづらい | 連続講座の回ごとの動員推移や累計をその場で見られない | イベント複製＋シリーズ単位の横断集計をダッシュボードに実装。**基本集計（申込数・充足率）は確定、シリーズ横断集計が候補** | △ 候補 | 中 |
| ⑥ | 参加者リストは件数が多いとメール受領になり、コンビニ/ATM支払い確認中の注文はフォームデータが反映されない | 名簿がその場で完全な形で手に入らない | ブラウザ内で即時CSV出力。キャンセル・待機を含む全状態を出力対象にする | **◎ 確定**（F-17に内包） | 高 |
| ⑦ | 有料チケットは4.9%＋99円/枚、振込210円/回、キャンセル返金は340〜500円/件。精算はイベント終了後5営業日以内で、終了日より前の振込は不可 | 手数料負担とキャッシュフローの拘束が発生する | 内製化により手数料が発生しない。※決済機能自体はスコープ外（Q1で確定）のため、提案根拠としてのみ記載 | × 対象外 | 参考 |
| ⑧ | 法人利用でアカウント管理者が退職しログイン情報が不明かつ登録メールが受信不可になると、アカウントの引き継ぎができない | 属人化したアカウントに全イベントデータが人質になる | データを自社保有し、JSONエクスポート／インポートで可搬性を確保 | **○ 予定** | 中 |

> **7/27 時点の実装確度（下方修正）**: 改善枠は他の必須機能に振り替えられ、**残っていない**。
> **確実に実装されるのは ①②⑥（必須機能 F-06 / F-07 / F-17 に内包）と ⑧（F-20・必須）の4件**。
> ⑤ は基本集計のみ確定でシリーズ横断集計は未定、③ は余力枠、**④ は取り下げ**。
>
> **提案の「短期の価値」（①②⑥）と「長期の価値」（⑧）は無傷だが、「中期の価値」（③⑤）はほぼ失われた。** 報告時はこの前提で説明する。

## 1.1 提案上の主張の組み立て

- **短期の価値**（①②⑥）: 日々の運用工数と事故リスクを減らす
- **中期の価値**（③⑤）: 自社ブランドでの継続開催と実績の可視化
- **長期の価値**（⑦⑧）: コストとデータ資産を自社に取り戻す

試験書もこの3層に沿って章立てすると、「何を保証したのか」が読み手に伝わりやすくなる。

---

# 2. Peatix 機能カバレッジマップ（T1-3b の成果物）

**「Peatixと機能が同じサービスを作れるか」への直接の回答。** Peatixの主要機能を棚卸しし、本サービスでの対応を分類する。

凡例: **◎** 同等機能を実装／**○** 主要部分を実装（一部に制約が残る）／**△** 制約により代替手段で実現／**×** スコープ外（理由を明記）

## 2.1 主催者向け機能

| # | Peatixの機能 | 対応 | 本サービスでの実現 | 機能ID |
|---|---|---|---|---|
| 1 | イベントの作成・編集・削除 | ◎ | 同等 | F-01, F-02 |
| 2 | 下書き／公開の切替 | ◎ | 同等 | F-03 |
| 3 | 開催日時・会場・定員・申込締切の設定 | ◎ | 同等 | F-01 |
| 4 | カバー画像・説明文の設定 | ◎ | 同等（画像はBlobで保持） | F-01, F-08 |
| 5 | 主催イベントの一覧・検索 | ◎ | 同等 | F-04 |
| 6 | イベントの複製（複数回開催） | ◎ | 同等＋シリーズ管理 | F-05 |
| 7 | 券種の作成・管理 | △ | 複数券種・券種別定員に対応。**有料券種は決済がスコープ外のため無料券種のみ** | F-23 |
| 8 | 申込フォームの追加質問設定 | ◎ | 同等（並べ替え・必須設定も可能） | F-07 |
| 9 | 参加者リストの閲覧・検索 | ◎ | 同等 | F-15 |
| 10 | 参加者リストのCSVダウンロード | ◎ | 同等（**即時出力・全状態を含む点で上回る**） | F-17 |
| 11 | 参加者へのメッセージ送信（一斉・個別） | △ | 文面生成＋宛先選択（個別／絞り込み結果／全員）＋`mailto:` 起動＋履歴記録。**自動送信・大量一斉配信は不可**。また**参加者からの受信（個別メッセージへの対応）はサーバがないため不可** | F-19, F-28 |
| 12 | 当日受付（チェックイン） | ◎ | 受付コード／一覧からの受付 | F-16, F-24 |
| 13 | キャンセル待ちの管理 | × | **対象外（F-14 廃止）。** 定員に達した券種は「満席」として選択不可とする | — |
| 14 | 売上管理・精算・振込 | × | **決済機能がスコープ外のため実装しない**（仕様書 8章 #1） | — |
| 15 | グループ（コミュニティ）ページ・主催者プロフィール・フォロワー | × | **参加者アカウント基盤が必要なため対象外**（仕様書 8章 #4） | — |
| 16 | アクセス解析 | △ | 申込状況・定員充足率・シリーズ横断集計のダッシュボードで代替（**閲覧数はサーバがないため取得不可**） | F-18 |
| 17 | イベントページのデザイン調整 | ◎ | 組織ロゴ・ヘッダー画像・紹介文の設定（**Peatixは非対応＝改善③**）。テーマカラーは廃止 | F-09 |
| 18 | 主催者アカウント・ログイン | △ | **パスコードによる簡易認証を実装。サーバ検証がないため実セキュリティは持たない**（仕様書 8章 #4） | F-27 |
| 19 | オンラインイベントの開催（開催形式の選択・視聴URLの案内） | ◎ | 会場／オンラインの開催形式選択と視聴URL欄を実装 | F-01 |
| 20 | イベントの検索カテゴリー設定 | ◎ | カテゴリを設定し、公開イベント一覧で絞り込める。**目的の「集客」に対応する機能として採用**（Peatixにあるからではない） | F-01, F-26 |
| 21 | ライブ配信（Peatix Live） | × | **動画配信基盤が必要なため対象外**（仕様書 8章 #8） | — |
| 22 | 定額課金（サブスクリプション） | × | **決済基盤が必要なため対象外**（仕様書 8章 #1） | — |
| 23 | 参加者選択機能（抽選イベント） | × | **実装自体は可能だが、5営業日の工数制約により今回は対象外。** F-14 の待機列機構を流用して拡張できる | — |
| 24 | アシスタントモード（受付担当への権限委譲） | × | **複数ユーザー・権限管理が対象外のため**（仕様書 8章 #4） | — |

## 2.2 参加者向け機能

| # | Peatixの機能 | 対応 | 本サービスでの実現 | 機能ID |
|---|---|---|---|---|
| 25 | イベントページの閲覧 | ◎ | 同等 | F-08 |
| 26 | イベントの一覧・検索（探す） | ◎ | 公開イベント一覧＋カテゴリ絞り込みを実装。**目的の「集客」に対応** | F-26 |
| 27 | 参加申込 | △ | 券種選択→入力→確認→完了までは同等。**枚数選択は対象外とし、1申込＝1名に限定**（`機能一覧.md` 参照） | F-11 |
| 28 | 定員・締切による申込制御 | ◎ | 同等＋重複申込チェック | F-12 |
| 29 | 電子チケットの表示 | ○ | **バージョン1・レベルL・英数字モードで自前実装**（受付コードとQRを併記）。**読み取りは対象外**で、受付は S-08 での受付コード検索による（仕様書 8章 #5） | F-24 |
| 30 | 申込履歴の確認（マイチケット） | △ | **メールアドレス＋パスワードでログインし、同一メールの申込を全件表示**（v1.17 で受付コード方式から変更） | F-25 |
| 31 | 申込のキャンセル | ◎ | 同等 | F-13 |
| 32 | 参加者アカウント登録・ログイン | △ | **メールアドレス＋パスワードによる本人確認で代替。** ただし平文保持・再発行なし・セッションなしで、アカウントとして成立しているわけではない（仕様書 8章 #10） | F-25 |

## 2.3 その他

| # | Peatixの機能 | 対応 | 本サービスでの実現 | 機能ID |
|---|---|---|---|---|
| 33 | データの持ち出し | ◎ | JSONエクスポート／インポート（**Peatixの課題⑧を解消**） | F-20 |

## 2.4 カバレッジ集計

**2026/07/28 時点。** 表の判定を機械的に数え直した値である。

| 対応 | 件数 | 割合 |
|---|---|---|
| ◎ 同等機能を実装 | 18 | 55% |
| ○ 主要部分を実装 | 1 | 3% |
| △ 代替手段で実現 | 7 | 21% |
| × 対象外 | 7 | 21% |
| **合計** | **33** | 100% |

**× 7項目の内訳**

| 理由 | 件数 | 該当 |
|---|---|---|
| サーバサイド基盤を要し、本課題の実行形態では**原理的に不可能** | 5 | 決済・精算／グループ・フォロワー／ライブ配信／定額課金／アシスタントモード |
| **工数上の判断**（実装可能だが期間内に収まらない） | 1 | 参加者選択機能（抽選） |
| **設計判断で廃止**（v1.14） | 1 | キャンセル待ちの管理（F-14。満席時は選択不可とする） |

## 2.5 結論（第一ステップへの回答案）

**「作成できる」。ただし条件付き。**

- 業務上の中核機能（イベント作成・公開・申込・定員管理・参加者管理・受付・名簿出力）は **33項目中18項目で同等機能を実装可能**。加えて電子チケットは**QRの表示まで自前実装**した（○1項目）
- △7項目は機能自体を実装するが、`file://` 単一ファイルという制約により**外部通信を伴う部分（実メール送信・QR生成・認証の検証）を代替手段に置き換える**、あるいは**仕様を限定する（申込枚数）**。**機能は存在するが、強度または範囲が本番と異なる**という位置づけ
- ×7項目のうち**5項目はサーバサイド基盤に依存**するもので、機能設計上の限界ではない。制約を外した本番実装での構成案は仕様書 8章に記載する。残る2項目は**工数判断（抽選）**と**設計判断による廃止（キャンセル待ち）**であり、いずれも実装できないわけではない
- 加えて、Peatix側の運用上の不便（1章 ①〜⑥⑧）については**同等ではなく上回る**設計とする

**要約すると、「日常のイベント運営に必要な機能は再現できる。再現できないのは、決済・動画配信・ユーザーアカウント基盤という、いずれもサーバを前提とする領域に限られる」** となる。

---

# 3. 不明点・確認事項リスト（T1-3 の起点）

回答状況（2026/07/27 12:45 時点）: **全8件 回答済。** 残作業は Q7 のサンプル入手のみ

| # | 確認事項 | 影響範囲 | 状態 | 回答・確定した判断 |
|---|---|---|---|---|
| Q1 | **有料イベント（決済）を扱うか** | F-23 券種設計、データモデル | ✅ 回答済 | **不要（モックアップ作成のため）。** 決済はスコープ外で確定。**券種は無料のみ**とし、`price` フィールドは持たない。仕様書 8章 #1 の通り文書化 |
| Q2 | 「機能が同じ」の範囲に**アカウント認証・実メール送信**は含まれるか | F-19, F-25, F-27, 仕様書 8章 | ✅ 回答済 | **含まれる。** → F-27 主催者ログインを追加、F-19 を必須へ格上げ。いずれも**代替実装＋本番構成案の提示**で応える（仕様書 8章 #2 / #4） |
| Q3 | **単一HTMLファイル／`file://` 起動の制約は確定か** | 全体 | ✅ 回答済 | **確定。** サーバサイドの使用は不可。仕様書 8章の制約がそのまま最終見解となる |
| Q4 | 評価者の**実行ブラウザ** | 動作保証範囲、試験書の前提条件 | ✅ 回答済 | **Chrome。** 他ブラウザは動作保証の対象外として仕様書・試験書に明記 |
| Q5 | **管理者側と参加者側の同一ファイル同居**の可否 | 画面構成、ルーティング設計 | ✅ 回答済 | **同居させる。** ハッシュルートで切り替える |
| Q6 | 想定する**イベント規模**（同時開催数・1イベントあたりの参加者数） | 一覧のページング要否、性能要件 | ✅ 回答済 | **「Peatixの仕様を真似るので内容を見れば分かる」。** → 規模の数値指定ではなく既定ルールの指示と解釈し、**方針P10**として明文化。**数十イベント／1イベントあたり数百名**を想定。**ページングは実装する**（当初は不要と判断していたが、デザイン案が数百件規模のページング前提であったため7/27に方針変更。F-04 / F-15） |
| Q7 | 結合試験仕様書の**フォーマット指定** | T4-4 | ✅ 回答済 | **以前 GanttForge で作成した形式に合わせる。** → **サンプル（エクスポートまたは画面）の入手が必要。7/29 までに確保する** |
| Q8 | 提出物の**納品形式** | T5-4 | ✅ 回答済 | **リポジトリ。** ZIPの作成は不要 → T5-4 の作業量が減少 |

## 3.1 残件

| 事項 | 対応 | 期限 |
|---|---|---|
| **Q7（試験書フォーマット）** | GanttForge で以前作成した形式の**サンプル（エクスポートまたは画面キャプチャ）を入手する**。入手できない場合は、項目番号・前提条件・手順・期待結果・結果欄の一般形式で作成する | 7/29（T4-4 着手前） |
| **Q9（自社運用の要件）** | **4章参照。** 現在ヒアリング中 | 7/28 午前（Day2の実装内容に影響） |

**確認事項に起因する設計判断のブロッカーは存在しない。** Q7 は試験書の様式のみ、Q9 は追加機能のみに影響し、既定の実装計画は進められる。

---

# 4. 自社運用の要件（Peatix起点でない機能）

方針 **P10・P11** に基づき、「Peatixにあるか」ではなく「**NCTのイベント運営に必要か**」で判断する機能を集める。
課題の目的にある「**機能的な面で不便がある**」に直接応える部分であり、**2章 カバレッジマップとは独立した検討軸**とする。

⏳ **2026/07/27 13:40 時点：ヒアリング中。**

## 4.1 候補（回答待ち）

| # | 候補 | 想定される価値 | 実装コスト | 備考 |
|---|---|---|---|---|
| H-1 | 参加者の所属企業・部署の把握 | BtoBイベントのリード情報として、名簿・集計で追える | **低** | F-07 の標準項目として定義すれば足りる |
| H-2 | リピーター・参加履歴の横断把握 | 同一人物の過去参加回数をイベントをまたいで見られる | 中 | `entries` をメールアドレスで横断集計。**「管理を便利に」に直結** |
| H-3 | 公開前の社内承認フロー | 下書き→承認依頼→承認済み→公開の段階を持たせる | 中 | F-03 のステータス拡張。**ただし承認者＝別ユーザーが必要で、認証制約（仕様書 8章 #4）と衝突する** |
| H-4 | 案内メールのテンプレート管理 | よく使う文面を登録し、イベントごとに差し込む | **低** | F-19・`settings` に内包できる |

## 4.2 確定後の進め方

- 実装するものを確定し、**`機能一覧.md` に F-36 以降として追加**する
- 原資は **Day4 T4-1 の改善枠**、または**改善③⑤の見送り**から確保する
- **H-1・H-4 は低コストで既存機能に内包できる**ため、採用されれば枠を増やさず実装可能
- H-3 は認証制約と衝突するため、採用する場合は「承認者の切り替えをパスコードで模す」等の代替設計が必要

---

# 5. 実機レビュー記録（Day2 / 2026-07-28）

Day1 に作成した仕様に基づき実装した `index.html` を実際に操作し、
仕様の妥当性を検証した。以下はその結果と、それに伴う仕様変更の判断である。

## 5.1 実機で判明した問題

| # | 観察された事象 | 原因 | 対応 |
|---|---|---|---|
| 1 | 管理側の「マイチケット」タブが全券種の集計表になっており、S-01 ダッシュボードと内容が重複していた | 参加者向けの概念（マイチケット）を管理側の画面として設計してしまっていた | **画面ごと削除**し、券種の状況は S-01 と S-04 で確認する |
| 2 | 参加者側にナビゲーションがなく、イベント一覧に戻る導線とマイチケットへの導線がなかった | 参加者側のシェルをヘッダー1本で済ませていた | ヘッダーに**3導線**（イベントを探す／マイチケット／主催者ログイン）を追加 |
| 3 | 管理側サイドバーで「公開ページを見る」「ログアウト」の表示が崩れていた | ナビゲーション項目と、性質の異なる操作（外部遷移・セッション操作）を同一のスタイルで並べていた | **ヘッダーを新設**し、性質で配置を分離。サイドバーはナビ専用とする |
| 4 | 申込完了画面の受付コードについて、何に使うのかが伝わらなかった | 「控えてください」としか書いていなかった | 用途（当日受付・申込確認・キャンセル）を明示し、マイチケットへの導線を設置 |
| 5 | 既定の申込フォーム項目に「所属部署」「年数」があった | 社内イベントを前提に設計していた | 自社主催であっても社員限定とは限らないため**削除**。汎用的な3項目に差し替え |
| 6 | 申込のキャンセルが受付コード1件ずつの照会でしかできなかった | S-11 を単票の照会画面として設計していた | 照合後に**同一メールアドレスの申込を全件表示**する形に再構成 |

## 5.2 マルチテナント化の検討と見送り

実機を操作するなかで、「他社や個人も主催者として利用でき、参加者が主催者を横断して
イベントを探せる形にできないか」という要望が挙がった。検討の結果、**本課題の構成では
実現不可能**と判断した。

**理由**: データは各PCのブラウザ内の IndexedDB に格納されており、
主催者間・参加者間でデータを共有する手段が存在しない。
A社のPCで作成したイベントは、B社のPCからは存在を認識できない。
同一の HTML ファイルを配布しても、内部のデータは共有されない。

これに連なる以下も、同じ理由により実現できない。

- 参加者・主催者のアカウント登録とロール分離
- 主催者グループの作成
- 主催者を横断した公開イベント一覧

**この検討により、Peatix の構造に対する理解が更新された。**
Peatix がイベント作成前にグループ作成を必須としているのは、
複数の主催者が同一プラットフォーム上でイベントを開催する**マルチテナント構成だから**である。
グループは主催者を識別する名前空間として機能している。
本サービスは単一組織での運用を前提とするため、この名前空間が不要となり、
イベントを直接作成できる。**機能不足ではなく、前提の違いによる構造の単純化である。**

（カバレッジマップ E区分「主催者：グループ」の判断根拠として本節を参照する）

## 5.3 改善④（キャンセル待ちの自動繰り上げ）の取り下げ

余力枠として確保していたが、実機レビューで判明した導線の問題（5.1 の #2・#4・#6）を
優先すべきと判断し、**対象外に変更**した。

改善①〜⑧のうち④を落とすため、**Peatix に対する改善は7点**となる。
満席時の挙動は「券種を選択不可とし、満席である旨を表示する」で成立しており、
機能として破綻はしない。

## 5.4 レビューを踏まえた実装方針

| 項目 | 内容 | 工数 |
|---|---|---|
| S-15 削除・サイドバー整理 | 5.1 #1・#3 | 0.25h |
| 管理側ヘッダー新設 | 5.1 #3 | 1.0h |
| S-11 マイチケット再構成 | 5.1 #6 | 0.5h |
| 参加者側ヘッダー3導線 | 5.1 #2 | 0.5h |
| 受付コードの説明改善 | 5.1 #4 | 0.1h |
| 既定フォーム項目の差し替え | 5.1 #5 | 0.1h |
| F-34 資料管理の実装 | 余力枠から前倒し | 1.0h |
| S-10 サービス概要の追加 | — | 0.3h |

**合計 3.75h。** 改善④の実装枠（1.5h）を上記へ振り替えることで、Day3 の枠内に収める。

---

# 6. 実機レビュー記録・第2回（Day2 / 2026-07-28・Issue #1）

リポジトリの Issue #1「改善点」および Issue #2「改善点：画像」で受領した指摘への対応記録。

## 6.1 不具合

| # | 事象 | 原因 | 対応 |
|---|---|---|---|
| 1 | 入力欄で日本語を打つと母音が落ち、子音だけが残る（「ハンズオン」→「ｈあｎｎｚうおｎｎ」） | 検索欄の `input` イベントごとに画面全体を再描画しており、**IME の変換確定前に入力欄そのものが作り直されて composition が壊れていた** | `compositionstart`〜`compositionend` の間は再描画しない。確定後に250msのデバウンスで反映し、フォーカスとキャレット位置を復元する |
| 2 | 充足率のバーがマウスホバー時に見えなくなる | バーのトラック色 `--color-surface-sub` が**テーブル行のホバー色と同一**だった | トラックを `--color-border` に変更（仕様書 9.11） |

> **#1 は「再描画で状態を作り直す」設計に起因する。** 同じ作りの検索欄が3画面（S-02 / S-08 / S-10）にあったため、共通の `searchInput()` に集約して一度に直した。

## 6.2 仕様変更をともなう指摘

| 指摘 | 判断 | 影響 |
|---|---|---|
| マイチケットを受付コードではなく**メールアドレス＋パスワード**で確認したい | **採用。** 受付コードは申込ごとに異なるため、「同一メールの申込を全件見る」という v1.16 の要件と両立していなかった | D-43 新設。S-11 の照合方式を変更し、仕様書 5.1 の注記と 8章 #10 を改訂 |
| イベント管理に**運営人員**をまとめたい | **採用。** 誰が責任者で誰が何を担当するかは運営の実務に直結する | **F-35 新設**（必須）。`events.staff` を追加、D-44 新設 |
| イベント作成を**数ページに分けたい** | **採用。** 入力項目が多く1画面では負荷が高い | S-03 を5ステップのウィザードへ再構成（機能の増減はなし） |
| ダッシュボードに**カテゴリ別の円グラフ** | **採用。** 外部ライブラリを使えないため SVG を自前で描画 | F-18 に内包 |
| メールテンプレートの差し込み文字列を**使えるようにしたい** | **採用。** 説明文に書いてあるだけで挿入できなかった | クリックでカーソル位置に挿入するチップを設置。F-19 に内包 |
| 公開イベント一覧に**画像を出したい** | **採用。** 文字だけで淡白だった | カード形式に変更し、左に 16:9 のカバー画像 |

## 6.3 参加者パスワード（D-43）についての注意

**これはアカウント機能ではない。** パスワードは `settings.participants` に**平文**で保持し、
端末内で照合するだけである。ハッシュ化も再発行もセッション管理もない。
主催者パスコード（F-27）とまったく同じ性質であり、**実セキュリティは持たない**。

仕様書 8章 #10 で「参加者・主催者のアカウント登録とロール分離は実現不可能」としている判断は変えない。
本実装は**その代替手段の強化**であって、実現不可能領域を解消したわけではない。

## 6.4 デザインの反映（Issue #2）

| 画像 | 対応 |
|---|---|
| **画像1（ACTIO ロゴ）** | 受領したのは背景ぼかし入りのモック画像（2.0MB）だった。そのまま埋め込むと灰色の矩形が出るうえ、base64 化で単一ファイルが 2.7MB 肥大する。外部URL参照も制約7で不可。**マークを SVG で描き起こし**、`--color-mark-facet` / `--color-mark-accent` をトークン化した。**元のベクターデータがあれば差し替える** |
| **画像2（デザイン参考）** | 「画像の通りのページを作る必要はない。雰囲気を真似てほしい」との指示に従い、**意匠のみ**を取り込んだ。サイドバーのアイコンと組織表示、アイブロウ、数値タイル、ヒーロー、行サムネイル、角丸と余白の拡大、影を外して罫線で面を切る方針（仕様書 9.10） |

> **参照デザインにある「閲覧数」「コンバージョン率」のタイルは作っていない。** 計測サーバがないため取得できず、
> 仕様書 8章 #7 で対象外としている。ダッシュボードは申込データから算出できる指標のみで構成する。

## 6.5 レイアウトの統一（Issue #1）

「管理者はサイドバー、参加者はヘッダーで統一感がない」という指摘に対し、
**サイドバーを廃止せず、意匠を共通化する**方針を採った。

CLAUDE.md §3 と仕様書 9.7 が「左サイドバー固定＋右コンテンツの2カラム」を定めており、
サイドバー廃止はこの規約の変更を伴う。一方、統一感の欠如は**ヘッダーの作りが左右で違うこと**に起因していたため、
**両側で同じ `.apphead` の高さ・ロゴ位置・字送り・アクティブ表示**を使うようにして解消した。

---

# 7. 実機レビュー記録・第3回（Day2 / 2026-07-28・Issue #3）

## 7.1 ダッシュボードの表示崩れ（不具合）

**事象**: FEATURED EVENT のヒーローで、カバー画像が画面いっぱいに引き伸ばされ、
本文カラムが潰れて**イベント名が1文字ずつ縦に折り返されていた**。

**原因**: `.hero` をフレックスで組み、画像を `flex: 0 0 42%` の項目にしていた。
**置換要素（img）の自動最小サイズは intrinsic size** であり、`min-width: 0` を持たないため
指定した 42% まで縮まず、本文カラムを 0 近くまで押し潰していた。高さの制限もなかった。

**対応**: `.hero` をグリッド（`minmax(0,1fr)` × `minmax(0,42%)`）に変更し、高さを固定。
タイトルと説明は2行でクランプする。**仕様書 9.10 に再発防止として記録した。**

> **この種の崩れを検知できていなかった。** headless Chrome では `file://` の IndexedDB が開けず
> 画面が描画されないため、自動検証はロジックまでしか見ていなかった。
> **検証用に IndexedDB のメモリ実装を差し込んだコピーを作り、実画面を撮影して確認する手順を追加した**
> （成果物 `index.html` には含めない）。

## 7.2 配色の変更

「背景色は白に」との指摘により、**暖色寄りオフホワイト（`#F7F6F3`）から白基調へ**変更した。
面をすべて白で統一し、階層は罫線で切る。管理画面のコンテンツ地のみ `--color-canvas`（`#FAFAFB`）で
わずかに沈め、カードとの前後関係を出す。テキストと罫線もニュートラル寄りに調整した。

CLAUDE.md §3 の「暖色寄りのオフホワイト基調」も「白基調」へ同期した。

## 7.3 参加者側の刷新

指定された構成をそのまま実装した。

1. ヘッダーの下に**ヘッダー画像を大きく表示**し、その上に「新しい知識と出会いが、未来をつくる。」
2. その下に**検索バー**（キーワード／開催形式／カテゴリ／開催日以降）
3. その下に**公開中のイベントを横並び**（上に画像・下に情報・**画像左上にカテゴリ**）
4. その下に**カテゴリから探す**（件数つきチップ）

> **「お気に入り」「通知」は作っていない。** 参照画像のヘッダーには含まれるが、
> 参加者アカウントを前提とする機能であり、仕様書 8章 #10 で実現不可能としている領域に当たる。

## 7.4 運用資料の共有についての記述

「デプロイの際は他のメンバーと共有されるものとして」との指摘により、
S-16 の注意書きを**「本番構成では組織メンバー間で共有される。本課題は `file://` 単体のため端末内に閉じる」**
に変更した。仕様書 2.4 の記述も同様に改めた。

---

# 8. QRコードの自前実装（Day2 / 2026-07-28）

## 8.1 判断を変えた経緯

Day1 の時点では「外部ライブラリを使用できないため QRコードはスコープ外」とし、
英数字8桁の受付コードで代替する方針だった（仕様書 8章 #5）。

Day2 に再検討したところ、**受付コードが8桁の英数字であることから
バージョン1（21×21）・誤り訂正レベルL・英数字モードで足りる**ことが判明した。
このバージョンに限れば**位置合わせパターン・バージョン情報・ブロック分割が不要**になり、
汎用のQR生成器を作らずに済む。生成多項式の係数と形式情報は検証済みの固定値を持てばよく、
BCH符号化や多項式の導出処理も実装しなくてよい。

**そのため「工数に見合わない」という当初の判断は、この用途に限っては成り立たなくなった。**

## 8.2 対象外として残したもの

**読み取り（カメラ受付）は対象外のままである。** 理由を「工数」から次の2点に改めた。

1. **`file://` はセキュアコンテキストではないため `getUserMedia` が使えない** — カメラを起動できない
2. デコーダは画像の二値化・射影補正・パターン検出を要し、外部ライブラリなしでの実装は工数に見合わない

**1がより本質的な制約である。** 仮にデコーダを実装しても、カメラが起動できない以上動かない。
これは**マルチテナント化を見送った理由（5.2）と同じ構造**で、どちらも
「データと実行環境が端末に閉じている」ことに帰着する。

## 8.3 検証の進め方

**「読み取れないQRは、無いより悪い」** ため、`index.html` に組み込む前に作業用の
`qr-test.html` で単体検証を行い、以下を満たしてから移設した。

| 検証 | 結果 |
|---|---|
| データ19語・誤り訂正7語が既知の期待値と一致（2件） | PASS |
| 生成 → 形式情報からマスクを復元 → デコードして元の文字列に戻る（8件） | PASS |
| 構造チェック（位置検出パターン・分離パターン・タイミング・固定黒モジュール） | PASS |
| **スマートフォンのカメラで読み取り、受付コードが表示される** | **PASS（実機確認）** |

> **符号化の誤りは見た目に現れない。** 誤り訂正やマスク選択が1ビットずれていても正常なQRに見えるため、
> 期待値との一致だけでは足りず、**実機での読み取りを完了条件とした**。
> 検証用に書いた逆デコーダは、エンコーダと解釈を共有している可能性があるため、実機確認の代わりにはならない。

検証を終えたのち `qr-test.html` は削除した（成果物は `index.html` のみ・制約2）。
技術詳細と再検証用の期待値は `QR実装メモ.md` に残す。
